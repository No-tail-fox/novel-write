Use case: ui-mockup.
Direction name: Timeline Desk.
Create a realistic, production-quality desktop application UI design with clear hierarchy, strong typography, intentional imagery and purposeful spacing. Output exactly one independent screen, 3840 x 2160 pixels, flat front-on, edge-to-edge application, no device frame, montage or perspective. Render a logical 1920 x 1080 desktop at 2x resolution. All typography and controls must be scaled together; layout dimensions below are proportional guidance, not fixed raster pixels. Recompose to fit this landscape viewport without stretching the UI or its media.

No reference images are attached to this text-to-image request. The specification below is grounded in existing StoryDream project-management and Director Desk pages. Use the neutral/coral visual system described here. Preserve StoryDream and the illustrative Chinese QA project 雨夜来信. Media concept: a woman in a charcoal scarf in a mountain village of pale stone houses, alternating with two people talking at a small wooden table. This is a UI concept, not a screenshot of implemented functionality or real user assets.

Product and goal: StoryDream Chinese desktop authoring for VOX documentary video and AI motion comics. This direction emphasizes the relationship of shot timing, narration and subtitles, with continuous preview and contextual properties. Keep the same visual family as project/library/task/settings pages. Show one focused workspace, not a feature inventory or marketing page. Do not include text explaining design decisions or keyboard shortcuts.

Visual system: canvas #f7f7f8, white work surfaces, #e2e5e8 dividers, #202326 primary and #5f6871 secondary text. #cf4938 coral for the primary command, active phase and playhead; #faeae7 selected rows. Small green/amber status signals. The media preview and track imagery can be charcoal #101316; surrounding toolbars remain light. Segoe UI/Microsoft YaHei-like type: 14px body, 13px controls, 12px minimum metadata, 16px tool heading. No negative tracking. Controls radius 6px; repeated asset thumbnails radius 4px. Consistent Lucide-like outline icons. Avoid gradients, glass, decorative shapes, cards inside cards, floating section cards, giant headings, tiny text, dominant purple/blue or beige/brown palettes.

Layout strategy: timeline-first horizontal authoring workspace. A fixed 56px project bar contains back arrow 返回项目, compact StoryDream, 雨夜来信, AI 漫剧; right side quiet 已保存 status and task/save/settings/more icons. A full-width 44px phase bar shows 文案 / 分镜 / 声音 / 交付 with 分镜 active. Preserve existing generation substeps through a compact contextual menu labeled 镜头生成, rather than a competing full second row of tabs. No permanent global application sidebar inside the editor.

Upper workspace is approximately 590px tall. A compact 208px left object pane contains the episode EP01 · 信从未来 and six numbered shot rows with small photographic concept thumbnails and durations; second shot selected with a slim coral leading line. A broad center area shows a large complete 16:9 preview of the mountain-village woman described above, accurate aspect ratio, no cropping off the frame. Toolbar above it has 镜头 02 · 异常出现, ratio and safe-area icons. Playback, timecode and sound tools are in their own row below the picture, never on top of the subtitle. A 320px right inspector aligns current-shot controls: header 镜头 02, 画面 / 动效 / 字幕 tabs, prompt textarea, motion dropdown, duration and seed lock. Advanced settings collapsed. One coral 生成当前镜头 button. Do not show every provider field or a large queue.

Bottom approximately 270px is a single full-width timeline work region separated with a horizontal rule, not a floating card. Header 时间轴 with zoom slider and compact fit, split and undo symbols. A precise time ruler from 00:00 through 00:40, six contiguous photographic shot clips on the 画面 track, teal-gray waveform on the 旁白 track, and restrained pale amber subtitle segments on the 字幕 track. Track labels remain at the left with mute/lock icons. One coral playhead corresponds to the selected second shot. The selected shot, preview and properties must agree visually. End with a slim bottom status row, no fake performance statistics. Timeline geometry is stable and naturally fits the viewport; do not invent unlimited editing capabilities or redundant tracks.

Only short accurate Chinese UI labels, no lorem ipsum, fabricated balances or arbitrary dates. Current-date anchor 2026-09-12, omit if not needed. Media is concept-only illustration based on the QA project described above. Differentiate sections by spacing, grouping, typography and simple dividers before any tint or borders. Maintain clear visual rhythm and deliberate whitespace while retaining a useful working density. No clipped text or overlapping controls.
