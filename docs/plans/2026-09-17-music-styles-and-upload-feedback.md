# 音乐风格与上传反馈

## 改动

- 参考 https://www.suno-api.io/create/ 于 2026-09-17 公开提供的标签数据，补齐 9 大类、46 子分类、633 条标签，保留全部中英文名称及分类；按英文去重后为 598 个不同标签。
- 高级创作与翻唱等再创作工具共用风格选择器，支持全局中英文搜索、多选、中文/英文写入、去重、移除和取消。合并时保留手写内容，检查 5000 字符上限。
- 关闭风格弹窗后恢复入口焦点；正常与紧凑窗口提供内容滚动和独立应用按钮。
- 解析嵌套音乐服务错误，将明确的曲库录音匹配拒绝显示为可理解的中文失败原因；未知服务器错误仍保留提交结果不确定性，刷新不自动重新上传。
- 修复音乐表单及详情区按钮高度被压缩、长错误信息换行和上传状态标题。

## 验证

- 风格数据与网页提取目录逐字段一致；实际 Electron 遍历 46 子分类共 633 条标签。
- 风格、创作表单及来源工具聚焦测试共 30 项通过；此前上传 Provider、运行时等 89 项回归通过。
- 完整类型检查与生产构建通过。
- `scripts/qa-music-layout.py` 使用隔离用户目录和离线接口夹具，在 1536×1024、1040×720 验证风格交互与嵌套翻唱弹窗，并在三个窗口尺寸验证音乐区按钮布局；零运行时错误。

## 已知限制

供应商对所测源文件的 JSON 上传返回空白 HTTP 502；文档所列 multipart 上传返回 HTTP 500 请求字段解析错误。本次未声称修复上游上传服务，未自动重试提交，也未触发付费生成。
