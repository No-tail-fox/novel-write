# 实施进度

更新日期：2026-09-16。用户已确认按首发范围实现，真实上线配置在部署阶段处理。完整交付说明见 [implementation-delivery.md](./implementation-delivery.md)；原 `implementation-plan.md` 保留设计阶段的目标与后续阶段，不代表所有阶段均已上线。

## 分支与隔离

当前功能检查点 `bdf1f71964bdd1e7d31e40810dd2402250496c4a` 已推送至 `origin/codex/shotcraft-motion-library`，设计检查点 `7c18c0ad889929b19ea9c1847f40b76e01f9d4dc` 已推送至 `origin/codex/account-credits-updates`。实施从设计检查点建立 `codex/account-credits-implementation`，在独立工作区完成，主工作区的导演台/VOX 改动不纳入本次交付。

## 首发实施状态

| 工作项 | 状态 | 已完成范围 |
| --- | --- | --- |
| 固定协议与 IPC | 完成 | 共享类型、Zod 参数校验、固定商业命令；renderer 不接触会话令牌、平台 key 或任意鉴权请求 |
| 账号与授权 | 完成 | 手机验证码适配、会话刷新/撤销、设备绑定/解绑、激活兑换、Ed25519 离线租约、账号切换隔离 |
| 钱包与任务 | 完成 | PostgreSQL 双分录账本、不可变报价、预留/结算/释放、幂等作业、私有产物下载、流水及重建对账 |
| 充值与退款 | 本地及协议验证完成 | 微信 Native v3 adapter、本地二维码、回调验签/解密、主动补查、整单未消费退款与冻结恢复；未接真实商户联调 |
| 桌面平台音乐 | 完成 | 普通、自定义和 Sounds 生成；报价确认、Max 倍率、候选列表、状态轮询、下载、试听及用于音乐 MV |
| 七类模型偏好 | 完成 | 同类多配置、保存与启用分离、账号隔离、平台/BYOK 明确来源、现有本机 key 引用；服务端包含七类 adapter |
| 其余平台创作管线 | 后续阶段 | 六类既有桌面创作管线的平台计费尚未迁移；高级音乐继续 BYOK，不静默切换来源 |
| 桌面商业页面与运营后台 | 完成 | 账号、钱包、软件授权、模型偏好、更新页面；独立运营台、RBAC、审计及敏感写操作的新鲜登录校验 |
| E1 更新 | 完成 | 签名完整包清单、来源/版本/大小/哈希校验、撤回复查、任务阻拦、手动升级；无自动安装 |
| 本地验收与交付文档 | 完成 | 下列自动化及界面验收通过，部署参数与上线边界已记录 |

## 验收证据

使用隔离 PostgreSQL 18 实例验证真实事务，测试库按随机名称创建并清理；没有使用用户真实支付或模型凭据。

- 服务端：27 个测试通过，0 跳过，含并发预留、设备席位竞争、刷新重放、支付/退款、账本与 worker 恢复。
- 桌面/renderer：13 个相关测试文件共 246 项验收通过。首次聚合的 2 项 preload 顺序失败已修复，对应 2 个文件复跑 71 项通过。
- 原生商业客户端 → HTTP → PostgreSQL → worker：7 组端到端检查通过，覆盖真实请求、两候选一单结算、重复支付通知、退款和跨账号隔离。
- 浏览器界面：深浅主题、1440/1040 宽度共 35 项检查，0 错误。
- Electron：使用独立测试资料目录连接本地服务，登录、safeStorage、激活、钱包、偏好启用、音乐报价/下载/试听及更新未配置状态检查完成，错误列表为空。
- 支付二维码：20 项专门测试和 14 项界面检查通过，界面检查无错误及外部请求；微信 Native 地址在本地编码，过期/已结算订单隐藏付款码。

renderer、Electron 和服务端类型检查通过；最终 renderer/Electron 构建通过。三个相关文件再验 123 项通过。发布签名脚本使用临时密钥完成 CLI 签名、客户端验证、完整包字节校验、任务保护和防覆盖 smoke；差异检查通过。

实现提交 `0bd252fd9da0ec51eeffad7af79733f500844d6b` 已按用户要求先推送到 `origin/codex/account-credits-implementation`，远程引用核验一致。后续补充交付文档与本地预览兼容修正：纯本地 HTML 视频预览不再解析生成模型配置，已有作品不因删除模型配置而无法预览；真正生成时仍执行账号来源校验。

## 部署阶段待办

生产短信、微信商户、上游付费模型、域名、发布证书及发布密钥尚未配置，也未发生外部发送、扣款、生成或生产部署。生产配置缺失时拒绝相应操作，开发 fixture 仅允许显式启用的 loopback 非生产环境。企业 MFA/IdP、S3 对象存储、NSIS 自动安装、断点续传与其余桌面平台计费管线留在后续阶段，不能按已上线功能发售。
