# 现有软件远程检查点

用户要求先上传当前功能，再在独立分支设计账号、激活、积分和更新系统。

## 本次归档范围

- Suno 音乐工作台、上传/克隆/高级创作/歌曲工具、素材库及 MV 接入。
- 文本、图片、视频、音乐、TTS、转写、视觉七类模型的多配置管理和独立凭据。
- 当前已在工作区完成的参考视频拆解、联网研究与内容抓取改进。
- 现有 HTML 场景结构和音乐 MV 编辑接口的桌面连接缺口、数据库保存与浏览器预览补齐。
- 对应测试、研究记录和界面验收材料。

本地 PPT、磁盘维护脚本、临时网页、运行数据、模型密钥和临时 QA 工作区不属于该提交。

## 验证

- 核心新增功能：27 个测试文件，364 项测试通过，全部模拟请求。
- 音乐及七类配置的浏览器交互、双主题正常/紧凑尺寸、设置往返和草稿恢复已验收。
- 提交前扫描暂存改动，未包含用户已登记的 API 密钥。
- 补齐旧桌面连接后，Electron 类型检查通过；存储与治理测试覆盖了重新打开、运行中和归档拒绝编辑。
- 扩展检查 371 项通过，1 项旧 VOX IPC 测试仍按旧 beatCount / totalDurationMs 输入断言，与现有创建合同不符；未为通过旧断言扩展产品接口。
- 现有 MV 完整编辑工作台的 2 项预写源码断言及命令清单的旧缺口不作为本次已实现功能；此次只补齐已有 API 的持久化与主进程入口。

## 分支顺序

1. 将本次提交推送至现有 origin 上游 codex/shotcraft-motion-library。
2. 核对远程哈希后，从该提交建立 codex/account-credits-updates。
3. 新商业功能设计与原型只进入新分支，不合并至 main。
