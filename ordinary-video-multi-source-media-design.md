# 普通视频分镜画面替换设计

## 1. 设计结论

普通视频保持现有生成链路，不增加“图片链路 / 视频链路”选择，也不增加新的任务类型。

系统仍然按现有流程生成分镜图片。图片生成后，每个分镜的当前图片都可以直接“替换画面”为视频：

- 上传本地视频
- 从视频素材库选择
- 从合格素材中随机匹配
- 基于当前分镜图片生成 AI 视频

替换是分镜级操作。哪个分镜被换成视频，哪个分镜在预览和剪映草稿里就使用视频；没有替换的分镜继续使用原图片。HTML 动画保持独立链路。

核心原则：

1. 图片是正常生成结果，也是视频失败或取消时的稳定回退。
2. 视频是当前分镜画面的替代媒体，不是另一套任务流程。
3. 替换必须就地完成，不跳转页面，不改变当前生成步骤。
4. 一个分镜只有一个“当前画面”，类型可以是图片或视频。

## 2. 场景媒体模型

字段名称用于说明产品合同，不代表已经实现：

```ts
type SceneMediaKind = 'image' | 'video';

type SceneVideoSource =
  | 'local-upload'
  | 'local-library'
  | 'local-random'
  | 'ai-video';

interface SceneMediaState {
  sceneId: number;
  generatedImagePath: string;
  activeKind: SceneMediaKind;
  activeVideo?: {
    id: string;
    source: SceneVideoSource;
    path: string;
    status: 'validating' | 'queued' | 'generating' | 'ready' | 'failed';
    durationMs?: number;
    trimStartMs?: number;
    trimDurationMs?: number;
    fit: 'cover' | 'contain';
    originalAudio: 'muted';
    generationId?: number;
  };
  candidateVideo?: SceneMediaState['activeVideo'];
}
```

`generatedImagePath` 始终保留。视频导入或生成时先成为候选，只有校验完成或用户确认后，`activeKind` 才切换为 `video`。

## 3. 页面入口

入口仍然是普通视频任务的“生成图片”步骤和“图片”标签，不在新建任务页增加媒体模式选择。

### 3.1 分镜卡片

每张已生成图片的卡片保留现有图片、场景标题、状态和“重新生成”，新增一个紧凑的“替换画面”按钮。

点击“替换画面”打开锚定在当前卡片上的菜单：

- 上传本地视频
- 从视频素材库选择
- 随机匹配视频
- AI 生成视频

菜单不使用四张大卡片，不把用户带到新的工作区。用户关闭菜单后，当前页面位置和分镜选择保持不变。

### 3.2 已替换状态

视频采用后，原卡片位置直接显示视频封面、播放按钮和时长，并把类型状态改为“视频”。卡片操作变为：

- 预览
- 裁切
- 更换视频
- 恢复图片

“恢复图片”使用该分镜原本生成的图片，不重新调用图片服务。

### 3.3 批量入口

页面工具栏可增加“批量替换”菜单，服务于大量分镜，但不改变单镜头的自然操作：

- 从素材库随机匹配选中分镜
- 为选中分镜生成 AI 视频
- 恢复选中分镜为图片

批量操作必须先进入多选状态，并在执行前显示影响的分镜数。

## 4. 三种替换方式

### 4.1 上传或选择本地视频

1. 用户在目标分镜点击“替换画面”。
2. 选择“上传本地视频”或“从视频素材库选择”。
3. 应用读取时长、分辨率、画幅、帧率、编码和文件摘要，并生成缩略图。
4. 剪映不兼容的视频转码为 H.264 MP4。
5. 校验期间原图片继续作为当前画面。
6. 校验通过后，在原卡片内预览候选，用户确认后替换。

本地视频复制到独立的受管视频素材库，不依赖原文件路径，也不并入人物素材库。

### 4.2 随机匹配视频

“随机”只在合格素材中选择，不对文件夹无条件抽取：

1. 按目标画幅、最小时长、文件可读性和编码兼容性过滤。
2. 同一任务内尽量避免重复同一片段。
3. 有标签时优先匹配场景语义，没有标签时才随机。
4. 没有合格素材时保留原图片，并就地提示原因。
5. 任务保存随机种子；重复打开结果不漂移，主动点击“换一个”才重新选择。

### 4.3 AI 生成视频

1. 用户在目标分镜点击“AI 生成视频”。
2. 默认使用当前分镜图片做图生视频，继承人物和构图。
3. 用户可在弹出的轻量侧栏里调整运动描述、模型和时长。
4. 排队和生成期间，原图片仍是当前画面。
5. 成功后先预览候选，点击“使用此视频”才替换。
6. 失败时提供重试、修改描述、选择本地视频，且不能破坏原图片。

异步结果使用 `generationId` 或修订号隔离。旧结果晚到时不能覆盖用户后续选择的视频。

## 5. 视频裁切与时长

场景目标时长继续由旁白和现有时间线决定，视频适配场景，而不是反过来改变旁白和字幕。

### 5.1 视频长于场景

- 默认从入点裁切到场景时长。
- 用户可调整入点。
- 剪映草稿使用 `sourceTimerange`，不破坏原视频。

### 5.2 视频短于场景

- 缺口不超过 800ms 时，可定格最后一帧补足。
- 缺口超过 800ms 时，默认继续使用原图片并显示“视频时长不足”。
- 第一版不自动循环人物或动作视频。

### 5.3 画幅和声音

- 复用模板的 `cover / contain` 适配。
- 画幅差异过大时显示裁切安全区。
- 视频原声默认静音，权威声音仍是旁白和 BGM。
- 转场、滤镜和模板边框继续按场景边界应用。

## 6. 状态设计

| 状态 | 卡片表现 | 当前导出画面 | 可用动作 |
|---|---|---|---|
| 图片已生成 | 图片缩略图，“图片” | 图片 | 替换画面、重新生成 |
| 视频校验中 | 图片保持，局部进度 | 图片 | 取消 |
| AI 视频生成中 | 图片保持，局部进度 | 图片 | 取消 |
| 视频候选可用 | 卡片内切换候选预览 | 图片 | 使用视频、放弃 |
| 视频已采用 | 视频封面、播放、时长，“视频” | 视频 | 裁切、更换、恢复图片 |
| 视频失败 | 图片保持，就地错误 | 图片 | 重试、改用本地视频 |
| 视频时长不足 | 图片保持，警告 | 图片 | 调整裁切、更换视频 |

错误出现在对应分镜和对应操作附近，并使用 `role="alert"` 或 `aria-live`，不能只显示页面顶部通用错误。

## 7. 剪映草稿规则

生成剪映草稿时，对每个分镜读取同一个权威状态：

```text
activeKind = video 且视频 ready -> 写入视频片段
否则                          -> 写入原分镜图片
```

- 视频片段写入裁切后的 `sourceTimerange`。
- 图片和视频可以自然混排在同一条普通视频时间线中。
- 草稿生成时冻结一次媒体快照。
- 草稿生成后再替换画面，交付区显示“画面已更新，需要重新生成草稿”。
- 视频失败、取消或未确认时绝不能让草稿缺少该分镜，继续使用原图片。

## 8. 交互与无障碍

- “替换画面”菜单使用稳定的图标和文字，不依赖颜色区分来源。
- 键盘可聚焦分镜卡片，Enter 打开卡片，菜单支持方向键和 Escape。
- 鼠标打开替换菜单不抢走正在编辑的提示词焦点。
- 视频不自动播放，默认静音，点击播放后才加载。
- 快速切换分镜时取消旧预览和旧异步揭示请求。
- 紧凑窗口中菜单转为底部操作面板，卡片宽度和主滚动保持不变。
- 使用项目现有 Lucide 图标，不引入第二套图标库。

## 9. 推荐实现阶段

### 阶段 A：本地视频替换与剪映导出

- 建立分镜当前媒体状态和独立视频素材库。
- 在现有分镜图片卡片增加“替换画面”。
- 支持上传、素材库选择、受约束随机匹配、裁切和恢复图片。
- 剪映桥接支持图片/视频混排和 `sourceTimerange`。

### 阶段 B：AI 视频候选

- 建立独立视频 Provider 和异步任务状态。
- 支持基于当前图片的图生视频。
- 支持候选预览、确认采用、取消、失败重试和旧结果隔离。

### 阶段 C：批量效率

- 多选分镜批量随机匹配。
- 多选分镜批量生成 AI 视频。
- 素材标签匹配、预计成本和撤销批量替换。

## 10. 第一版范围

第一版包含：

- 正常图片生成链路不变
- 单分镜就地替换画面
- 本地视频上传、素材库选择和受约束随机匹配
- 图生视频
- 视频裁切入点
- 原声静音
- 随时恢复原图片
- 剪映草稿图片/视频混排

第一版不做：

- 新建任务时选择图片/混合/视频模式
- 独立“视频任务”类型
- 一个分镜内多段视频拼接
- 视频原声混音
- 复杂变速曲线
- 多轨视频叠加
- 完整非线性编辑器

## 11. 验收标准

- 旧任务和正常新任务继续按原流程生成图片，行为不变。
- 每张已生成分镜图片都有明确的“替换画面”入口。
- 用户无需离开当前步骤，即可上传、选择、随机匹配或生成视频。
- 替换完成后原卡片位置直接显示视频；恢复后直接回到原图片。
- 视频处理期间和失败后，原图片始终可用且不会被覆盖。
- 预览、卡片类型、裁切状态和剪映草稿读取同一个当前媒体。
- 图片与视频能稳定混排进同一个剪映草稿。
- 修改画面后，旧草稿明确显示需要重新生成。
- 桌面和紧凑窗口无控件裁切、重叠或不可达操作。
- 所有替换、生成、候选、采用、失败和恢复状态可被键盘及辅助技术识别。
