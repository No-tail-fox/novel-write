# 普通任务草稿预览一致性修复计划

## 目标

让普通任务的应用内草稿预览尽可能按剪映草稿的真实参数显示，修复图片区域、主标题字号/换行/下划线和底部字幕与实际草稿明显不一致的问题。

## 范围

- 产品模式：桌面内容生产工具
- 变更范围：普通任务草稿预览窄修复
- 不修改 HTML 动画链路
- 剪映仍是最终渲染器；应用内预览使用同一模板参数做等比例模拟

## 阶段

- [complete] 1. 对照用户截图与最近“文在寅”任务定位差异来源
- [complete] 2. 统一剪映文字单位、描边和下划线的预览换算
- [complete] 3. 让场景预览显示真实分时字幕 cue，而不是整段旁白
- [complete] 4. 校准浏览器图片区域与剪映缩放/蒙版几何
- [complete] 5. 补充单元与桌面视觉回归测试
- [complete] 6. 运行完整测试、类型检查和构建

## 验收标准

- 同一场景、同一字幕时间点比较时，图片素材和字幕文案一致
- 主标题在预览与剪映中的相对字号和换行数量接近
- 标题下划线、描边、字幕背景在缩放画布中保持可见
- 图片区域的顶部、底部和 `cover` / `contain` 行为与模板参数一致
- 普通任务预览明确显示当前字幕 cue，不再一次性展示整段场景文案

## 错误记录

| 错误 | 次数 | 处理 |
|---|---:|---|
| 初始误沿 HTML 动画链路排查 | 1 | 用户纠正后切换到普通任务 `DraftCanvas` 与 `pyJianYingDraft` 桥接 |
| PowerShell 通配符传给 `rg` 导致 Windows 路径错误 | 2 | 改用 `rg -g` 或明确文件路径 |
| PowerShell 原生命令内联 Node 引号被剥离 | 2 | 改用 UTF-8 here-string 通过 stdin 执行只读查询 |
