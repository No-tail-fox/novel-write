# 普通任务草稿预览一致性发现

## 截图与任务实物

- 两张截图不是同一场景：应用预览显示场景 5“父亲四处打零工…”，成片截图显示场景 7 的字幕 cue“普通人在生活重压下的”。
- 最近“文在寅”任务使用同一个自定义“标准模板”，模板在任务生成前已保存，排除模板后来被修改导致的差异。
- 模板画布为 1080×1920，图片区域 `top=0.13818`、`height=0.64`，标题字号 34、宽度 0.8、下划线开启，字幕字号 14、每行 12 字、黑色半透明背景。

## 根因

- `DraftCanvas` 将剪映 `TextStyle.size` 直接当 CSS 像素使用，但预览画布只有约 300px 宽；剪映单位与 CSS 像素不是同一尺度。
- 前端预览直接显示整个 `scene.cap`；真实草稿通过 SRT 拆分并按时间一次显示一个短 cue。
- 标题下划线使用浏览器默认 `text-decoration`，在粗描边和缩放后不稳定，不能可靠模拟剪映下划线。
- 浏览器图片使用 `object-fit`；桥接使用素材缩放、位置和矩形蒙版。参数相同但实现分离，需要共享可测试的几何结果。

## 技术边界

- 剪映没有提供可嵌入的实时渲染 API，因此应用内预览无法做到逐像素等同；可以通过统一参数换算做到布局和文案一致。
- 比较必须使用同一场景和同一字幕时间点，否则图片和字幕内容变化属于正常时间线行为。

## 最终处理

- 大预览和草稿模板编辑画布改用随画布宽度缩放的剪映字号换算；模板列表的小缩略图保持原有紧凑字号。
- 描边改为随画布响应式缩放，并明确设置下划线粗细、偏移和穿字规则。
- 任务预览按当前场景读取真实字幕 cue，底部提供上一条/下一条切换及 `字幕 n / N` 状态。
- 剪映画框即使关闭，只要图片区域不是全屏，也会生成使用画布底色的裁切覆盖层，防止图片蒙版越出模板区域。
- HTML 动画链路未参与本次实现。
