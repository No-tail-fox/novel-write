# 研究发现

## 2026-09-12 导演台概念设计约束

- Image 2.5 已成功生成两张概念图：方案一突出大预览与右侧属性，方案二突出六镜头分镜总览；两者均符合请求尺寸且未本地缩放。生成图的中文文本存在少量字符质量风险，正式 UI 文本必须由 React/CSS 渲染，不直接使用概念图作为产品资产。
- 时间轴方案超时且计费状态未知；没有继续提交同一请求，避免在账单未确认时重复调用。
- 用户新指定的 `https://ai.input.im/v1/images/generations` 在本次同一模型/密钥请求中也返回 `503: No available compatible accounts`。两个域名返回相同服务错误，但未据此推断它们一定共享同一后端；需要服务侧确认生图通道可用性。
- 后续使用用户本次明确提供的密钥请求文字生图 `/v1/images/generations`，服务返回 `503: No available compatible accounts`，不是图片产物。旧环境密钥请求返回 502，不能将旧环境认证/调用结果混同为本次凭据的能力验证。三套提示词现已明确不附带图片，仅按既有设计规范生成。
- `MASTER.md` 的 Active Implementation 是当前视觉依据，旧蓝/橙色自动建议不再适用。以项目首页、导演台实际截图作为参考，不将示例媒体写入用户项目。
- 三个待生图方向只改变布局侧重点，共用项目顶栏、阶段导航、中性表面、珊瑚选中态、系统中文字体和 Lucide 图标；提示词保留版本、审片与高级设置入口。
- Product Design 要求生成后展示三张独立方案并等待用户选择，不能直接从文字提示词开始改代码。
- 前次使用既有环境凭据的 `/v1/models` 可认证且列出 `gpt-image-2`，不等于带图编辑链路可用；当时 `/v1/images/edits` 返回 403/502。后续纯文字生成接口结果见本节顶部。API 凭据仅放在调用进程环境，不写项目文件。

## 2026-09-12 共享视觉审计

- 当前共享壳层包含重复项目导航，紧凑侧栏账户按钮被旧 grid 拉高，Fluent 仍有默认蓝色选中线；优先在共享层解决。
- 采用中性灰白工作区、白色内容面、珊瑚操作色、单一无衬线字族和细分隔线；保留媒体真实预览和语义状态颜色。
- ui-ux-pro-max 搜索脚本需显式加入 sys.path 才能由仓库嵌入式 Python 运行；使用本地导航/一致性指导，不采用偏营销的滚动叙事推荐。
- CDP 字体验证为 Microsoft YaHei UI，非真正衬线回退；视觉细碎来自小字和层级混杂。共享字体变量与更清晰的控件字号已统一。
- 新增跨页面 QA 发现任务操作页仍有高优先级 73px 页头，已统一为桌面 88px/窄屏 80px；图标分段控件沿用 IconButton 的 title 与 Fluent Tooltip 双支持。
- 1080x720 的 Electron 重叠检测发现 140px 类型卡片会被 sticky 步骤栏遮挡；紧凑模式改为 96px 横向卡片，760px 以下 76px，复跑 8 个壳层捕获通过。
- 窄屏设置的旧 11 项菜单和四列配置卡导致字段被挤压；分类改用同一 state 的 SelectField，操作区换行、配置卡两列加独立操作行，编辑表单单列。
- 最终证据：36 张浏览器截图无页面错误/文档横溢，233 项回归通过；Electron 壳层 8 个捕获无对比度/焦点/重叠失败，500 项管理 15 组操作通过。浏览器预览不代表真实生成服务可用。

## 2026-09-12 shell 与 P01 验收发现

- 六入口是分组导航而不是删除页面：六个一级入口各有默认视图，当前入口展开其所属旧视图；账户、激活等功能仍保留可达路径与底部入口状态。
- 项目卡需要区分“徽标短文案”和“状态详情”：`pending` 徽标为“等待”，详情为“等待开始”；两者语义不同，不能把运行中的步骤文案复用到等待态。
- 对比度 QA 能捕捉运行时真实背景混合结果。`pending` 首轮在浅色项目页为 4.43:1，修复为警示色组后，最终四张项目捕获均无文本或焦点对比度失败。
- shell 夹具只播种项目数据和草稿模板，不启动 scene-video/FFmpeg/Python 依赖；这使 P01 双主题、双视口的验收更短，也避免为纯壳层测试引入媒体副作用。
- 最终报告中的 `projectStatus.ready` 不能只依赖卡片存在；当前同时校验卡片数量、七状态文案和等待卡不包含“当前步骤”。


## 2026-09-10 StoryDream 双端重构

- 共享 React renderer 和 StoryDreamApi 已存在；浏览器 fallback 含模拟数据，需服务层适配后才能作为正式 Web 版。
- 全局壳层与导演台各有布局，重构需同时关注路由、未保存状态与上下文面板，不能只换颜色。
- input.codes 公开页面列为 AI.INPUT.IM 入口；专用生图技能有 `/v1/images/generations` helper，凭证仅置于进程环境。
- 浅灰白/石墨/珊瑚方向及本机 Web 优先架构详见 `docs/plans/2026-09-10-web-ui-rebuild.md`。
- 生图复核：早期 `gpt-image-2` 返回 502/503、gpt-image-1 返回不支持；最新一轮明确 string 类型与 UTF-8 字节请求后，ai.input.im / gpt-image-2 成功返回项目首页和中文编辑器两图。图像路径、原始尺寸与证据边界见 `docs/design/storydream-web-redesign/review.md`。

## 2026-09-10 页面重构详细方案

- 新增 `docs/plans/2026-09-10-page-reconstruction-spec.md`，作为逐页实施细则；总路线文档只保留 R0-R9 架构总览。
- 方案把 20 个现有视图完整映射到项目、素材库、灵感、模板、任务、设置六个主入口；账户/激活放到底部菜单，MV 作为第五种新建类型，未删除旧路由。
- 方案定义应用外壳与项目编辑器两种布局、四阶段导演台、20 个页面规格、对象归属和移动端面板切换，明确哪些历史能力必须保留。
- 方案明确 browser fallback 仅为预览；正式本机 Web 需要 loopback companion、一次性配对、能力白名单、事件恢复和受管媒体 URL。剪映的草稿生成、启动、失败和无连接器下载包分别报告。
- 本轮没有把公网服务、真实账户、远程任意文件访问或未完成 hybrid 策略纳入页面重构的必选交付。

## 附件
- PPT 模板：`H:\xwechat_files\wxid_8z59e591g3qj22_dc7f\temp\RWTemp\2026-08\4782b1d834bee796fd5c3127456f9147\【模板】第二批数据标注先行先试工作（领域创新）申报答辩(2).pptx`
- Word 方案：`H:\xwechat_files\wxid_8z59e591g3qj22_dc7f\temp\RWTemp\2026-08\7f4bccf1cabe1f52430725331c4158a0\0812定稿-平顶山市数据标注产业先行先试建设方案(1)(1).docx`

## 当前判断
- 用户要的是大纲/内容规划，不是立即生成 PPTX；不触发 PPTX 写作与导出。
- 模板与方案都必须做结构化抽取；不能只凭 Word 目录或模板封面推断。

## 模板结构（PPTX）
- 14 页，16:9（13.333 x 7.5 in）。模板布局库包含目录、章节页、内容页、结束页等多种母版。
- 页 1：标题“第二批数据标注先行先试工作（领域创新）申报答辩”，副标题 XX省XX市、2026年8月。
- 页 2/4/7/11/13：CONTENTS 目录页，五章为基础条件、建设思路与目标、重点任务、保障措施、特色亮点。
- 页 3：基础条件 1-2 页，必须覆盖行业资源优势、应用场景优势、标注产业基础、科技创新、区域优势、企业集聚、技术创新、基础设施、政策环境。
- 页 5：建设目标 1-2 页，必须含发展定位、总体目标、创新能力、高质量数据供给、行业赋能量化目标。
- 页 6：建设思路 1-2 页。
- 页 8：总体建设任务总述，要求对接国家政策、本地产业基础、区域战略、重点依托单位，概括四个先行先试任务。
- 页 9/10：建设任务拆解页；页 12：保障措施；页 14：特色亮点，可多页。
- 版式事实：内容页标题占位符约 24 pt，目录项约 28 pt，目录“CONTENTS”约 100 pt，正文页需采用高密度卡片/流程/矩阵而非照搬模板说明文字。

## Word 方案结构与事实
- 正文 92 个段落、1 张指标表、2 个节；标题顺序：基础条件 → 建设思路与目标 → 重点任务 → 保障措施 → 附件。
- 基础条件 7 组：
  1. 煤炭化工产业链完整、行业特色场景丰富：70 余年产业积淀，煤炭开采—煤焦化—盐化工—尼龙新材料—新能源储能完整链条；44 个智能化采煤工作面、14 个省级智能化矿井；透明地质、智能开采、灾害预警、化工工艺、设备维护、安全管控、新能源运维等场景。
  2. 能源数字化基础扎实：5000TB+ 原生工况数据；59 个市直部门、12 个县区共享渠道；数据交换超 90 亿次、公共数据超 15 亿条；14 个行业数据集；天信、磐石、豫智工业大模型；工业互联网综合云服务平台和标识解析二级节点。
  3. 数据标注产业快速发展：相关企业 38 家，年营收近 3 亿元，从业人员超 5000 人，标注总规模超 600TB；三大载体；6 个分布式处理中心；接入 70 余家头部企业供应商体系、合规率 95%+；智数科技对接上下游企业 170 余家；平煤人力/智数科技双链主。
  4. 科创体系梯次完备：白龟湖科创新城 1.59 万家市场主体、33 个省级以上平台；全市 350 家创新平台、438 家高新技术企业、11 家专精特新“小巨人”、创新型企业超 1000 家；关键技术、4 项专利/软著、196 个省部级以上项目；高校—科研院所—企业协同。
  5. 数字基础和人才：5G 基站 1.11 万个、5G-A 4109 站、千兆光网端口 334.03 万个、3 个省级工业互联网平台、服务 1.4 万家企业/接入 240 余万台套设备；高技能人才近 40 万、持证率 46.3%；24 万职工转岗体系；7 所高校相关专业；培训 6000+ 人次、骨干 500+、合格率 90%+；招聘近百场、岗位 3000+、专家 20+。
  6. 政策环境：省级“人工智能+”行动计划及支持政策；市级数据产业行动计划、数据标注产业高质量发展行动方案；纳入“十五五”和“七群十二链”；市委市政府工作专班、专项资金、白龟湖科创新城 18 条专项政策。
- 建设思路与目标：指导思想、三重发展定位、四个核心优势、短中长期发展思路；2029 年总体目标为“全国智慧能源（煤炭化工）数据标注中心”，并形成产业/企业/技术/人才/场景/流通六维格局。
- 2029 总体量化目标：企业 180 家以上、营收 10 亿元以上、标注规模 1500TB 以上、关键技术 17 项以上、人机协同效率较 2025 年提升 50%+、智能质检准确率 96%+；高质量数据集 15 个以上、国家系统登记 10 个以上；行业应用场景 30 个以上、服务企业/机构 150 个以上。
- 重点任务共 7 项：技术创新攻关；行业共性标注平台；高质量数据集供给；梯度化企业培育；人才引育与标准；多元行业场景赋能；数据要素流通与安全合规。
- 四轴映射：A 先进标注技术攻关 = 任务 1+2；B 高质量数据集供给 = 任务 3；C 产业提质发展 = 任务 4+6+7；D 人才与标准建设 = 任务 5。
- 保障措施 4 组：组织领导；政企联动；产业监测；资金和要素保障。用户要求的政策标准、资金支持、人才支持将在保障页显式列出，并与任务 5、政策环境事实互相指引。
- 四个核心优势原文必须显式出现：龙头企业实力雄厚；行业数据资源稀缺独有；区位成本优势突出；产城教深度融合、专业队伍适配性强。
- 可选额外亮点：全国智慧能源（煤炭化工）垂直赛道定位；“场景资源+行业数据+垂直大模型”闭环；“国企资源+民企技术+园区载体+政府服务”四位一体；产业工人转岗与产教融合；“头部技术+本土场景+平煤资源”合作范式；资源型城市可复制示范。

## QA 备注
- PPTX 结构化转换成功，Markdown 共 14 页，未发现转换警告。
- Word `render_docx.py` 因环境缺少 LibreOffice/`soffice` 失败；已完成文本、表格、段落结构核验，未完成 PNG 视觉核验。

## 补充材料目录盘点（2026-08-19）
- 根目录：`H:\xwechat_files\wxid_8z59e591g3qj22_dc7f\msg\file\2026-08\新建文件夹\新建文件夹`
- 共 15 份，约 509.42 MB：`.doc` 8 份、`.docx` 2 份、`.wps` 1 份、`.pdf` 1 份、`.pptx` 3 份。
- 本机 Microsoft Word 16.0 COM 可用，可只读抽取旧 `.doc`、`.docx` 和 `.wps`；PDF 使用 `pypdf/pdfplumber` 和页面渲染；PPTX 使用 `python-pptx` 与 PPT Master 转换能力逐页盘点。

## 补充材料首轮发现
- 11 份 Word/WPS 全部只读抽取成功；PDF 25 页全部渲染检查，页面无明显缺失或不可读；PPTX 共 26+76+16=118 页，均完成逐页文本和媒体索引。
- 25 页 PDF 由三份报告合订：市级创建数据标注产业创新实验区汇报、平煤神马集团数据产业发展专题、新华区数据标注产业专题。内容高度相关，但主要为 2026 年 5 月口径，数值与 8 月定稿方案冲突时必须以后者为准。
- 可考虑直接补强的事实：2646 个标准化标注席位；“一基地连一城”协同格局；已试点建成 10 余个人工智能应用场景；3 家企业获评河南省首批数据企业、1 家入选河南省数据资源开发利用试点；平顶山为全省首批产教融合试点城市。
- 需谨慎或不直接使用的旧口径：企业 25 家、营收 2.14/3 亿元、从业 3300 人、500TB、技术骨干 143 人、培训 2000 人次等，均已被 8 月方案的 38 家、近 3 亿元、5000+ 人、600TB+、500 人、6000+ 人次等更新口径覆盖。

## 补充材料最终采用结果
- 主大纲仍为 25 页，不增加章节过渡页。
- 新增“已有能力/成效”证据：2646 席、约 100 个重点项目、约 3200 万条交付、10+ AI 场景、分钟级预警、回收率提升 1.5 个百分点、化工能耗降低 5%-8%。
- 新增试点资质：中国平煤神马 2023 世界 5G 大会十大应用案例（全省唯一）；3 家省首批数据企业、1 家省数据资源开发利用试点；河南省首批产教融合试点城市。
- 新增可复制模式：“一基地连一城”“2-1-N”分布式协同、企业全生命周期服务、中国信息协会数据标注专业委员会组织基础。
- 视觉素材来源已精确到旧 PPT 页码；仅抽取照片、地图、产业链和平台示意，禁止截图整页和继承旧数字。
- 高风险/未进入最终方案的内容已排除：海外 300MW 智算中心、英伟达 CEO 合作、高端芯片采购授权、液冷 25 倍/PUE 1.15、1000TB 资源池、8-10 数据集、3-5 模型等。

## 官方模板与内页参考课件的视觉融合结论
- 两套均为 16:9（1280x720）。官方模板主题主色 `#C5000C`、白底、红色目录页，主题字体中文标题为微软雅黑、正文为黑体，主题字号 44/28/24/20/18 pt。
- 参考课件的长处是高密度信息组织：顶部结论条、2-4 栏模块、图片证据墙、数字卡、全宽表格、流程图、任务/保障分组、页内明确章节编号。
- 参考课件的深蓝背景、紫色章节标签、黄色大面积高亮与官方红白系统冲突，不继承；其大量 10-12 pt 正文也不适合 8 分钟现场答辩，不照搬。
- 融合策略：官方模板控制封面/目录/页眉/配色/字体，参考课件只控制内页结构和密度。常规正文 16-18 pt，关键数字 24-34 pt，说明/来源 11-12 pt；第 11 页完整年度指标表允许 13-14 pt。
- 每页保持一个结论、3-5 个信息模块、约 35%-50% 的图形/数字视觉面积；高密度来自模块化和视觉编码，不来自缩小字号或整段堆字。

## 最终 PPTX 生成与验收
- 输出：`I:\opc\平顶山市数据标注先行先试申报答辩.pptx`。
- PPT Master Quick Generate 项目：`I:\opc\projects\pingdingshan-data-annotation-defense_ppt169_20260819`。
- 25 页、16:9；271 个顶层幻灯片对象，SVG 转换为原生 DrawingML 可编辑形状，图片保持为嵌入媒体。
- final SVG checker：25 页、0 blocking error；仅保留顶层静态元素分组与封面图片放大等非阻塞警告。
- `svg_to_pptx.py` postflight：passed-with-warnings，质量门通过，25 页全部转换成功。
- PowerPoint COM 成功渲染 25/25 页为 1280x720 PNG；联系图及 P01/P11/P18/P23/P25 放大检查未见重叠、截断或缺图。
- 成品反向文本抽取为 25 页；四个任务轴、四大优势、2029 关键目标和 14 项年度指标均通过检索核验。

## v4 大纲内容重排
- 用户指定 `8分钟25页亮点扩展版大纲_v4.md` 为最终内容权威，既有样式、图片和高密度结构保持。
- 新结构：P01-P20 为 7 分 45 秒主讲线；P21-P25 为现场有余量或答辩追问时使用的亮点展开页。
- 五个章节目录页恢复至 P02/P06/P10/P15/P18；P03-P05 基础条件，P07-P09 思路目标，P11-P14 七任务，P16-P17 保障，P19-P20 主亮点。
- P08 完整保留 14 项年度指标，并补 2029 总体目标带；明确“累计技术成果 17 项+”与“年度核心技术突破 10 项”为不同口径，数据集 PB 值不编造。
- P21-P25 分别为：全链条场景深耕、工业与公共数据双底座、三载体与双链主交付、产城教融合转岗、可信流通安全合规与示范输出。
- 当前最终输出：`I:\opc\平顶山市数据标注先行先试申报答辩_v4最终修订版.pptx`；旧版备份：`I:\opc\平顶山市数据标注先行先试申报答辩_v1备份.pptx`。
- 最终 SVG gate：25 页、0 blocking error；PPTX postflight 通过；PowerPoint 渲染 25/25 页通过，重点页放大复核未见截断或重叠。

## DB 新汇报稿结构（最终内容权威）
- 源文件：`平顶山市数据标注先行先试汇报稿DB.doc`，7 页、3187 字、38 段，无表格/图片。
- 五章结构：基础条件；建设思路与目标；六大重点任务；保障措施；特色亮点与结语。
- 基础条件严格为四项：产业链条与行业统筹；稀缺数据与数字基础；产业集聚与承载；人才与科创环境。
- 思路目标：不拼通用赛道的量，只争垂直领域的质；打造全国智慧能源数据标注中心；三个定位；短中长三步走；2029 六项核心指标。
- 重点任务严格为六项：技术创新及共性平台；高质量数据集；企业梯度培育；人才引育；多元场景赋能；数据要素流通。
- 保障严格为三类：组织领导与季度/年度评估；政企联动和产业联盟；政策资金支持。
- 特色亮点严格为四项：垂直深耕非规模扩张；双链主协同；数据-模型-场景-迭代闭环；以数促转的资源型城市转型路径。
- 新稿不要求上一版的七任务和 14 项年度指标，DB 版本不得继续沿用这些内容。
- 页面方案：20 页，适配 8 分钟；封面/目录 2，基础条件 5，思路目标 4，重点任务 5，保障 2，亮点与收束 2。
- 最终输出：`I:\opc\平顶山市数据标注先行先试申报答辩_DB新纲版.pptx`，20 页、16:9、约 3.31 MB、原生可编辑 DrawingML。
- PPT Master final gate：20 页、0 blocking error；postflight 包完整性、页数、图片、字体和质量门均通过。
- PowerPoint 成功渲染 20/20 页；联系图及 P01/P05/P07/P09/P11/P13/P15/P17/P19/P20 放大检查无截断、重叠或缺图。
- 成品反向核验：五章 5/5、任务 6/6、保障 3/3、亮点 4/4、2029 七项核心目标、支持指导/实地考察/汇报完毕均存在。
- 备注增强版：`I:\opc\平顶山市数据标注先行先试申报答辩_DB新纲版_含解说备注.pptx`。
- 每页备注 264-289 字符，平均 276.4，20/20 覆盖；内容均为可直接朗读的忠实解说，无舞台提示或新增事实。
- Native Enhance postflight：只启用 notes，写入 20 个 notesSlides；audio 0、自动翻页关闭、额外转场关闭，原有 fade 转场保持。
- 源版与备注版经 PowerPoint 重新渲染后，20/20 页 PNG SHA-256 完全相同，证明可见内容、样式、图片和布局未改变。
- 20 页完整备注合计约 5500 字符，若全部照读将超过 8 分钟；8 分钟现场应优先讲每页首句结论和核心数字。

## DB-v1 亮点缺失诊断
- `DB-v1.doc` 修改时间 16:17，晚于生成 DB 版所依据的 `DB.doc`（13:54），但两份文档的正式亮点数量均为 4 条：垂直深耕、双链主协同、数据-模型-场景-迭代闭环、以数促转。
- `DB-v1.doc` 没有新增第 5/6/7/8/9 条亮点；这五条来自更早的 `8分钟25页亮点扩展版大纲_v4.md`：全链条场景、工业/公共数据双底座、三载体/双链主交付、产城教/工人转型、可信流通/安全/示范。
- 用户要求“严格按照 DB 新结构”后，PPT 从 v4 的 25 页改为 DB 的 20 页，P21-P25 五张扩展亮点页被删除；这是当前“新增亮点不全”的主因。
- 当前 DB PPT 的 P19 虽含 4 条正式亮点，但为 2x2 压缩表达：第 2 条缺少“稳定行业专家团队、规模交付、市场化合作”；第 3 条缺少“跳出传统外包、推动产业转型”；第 4 条对“全国同类城市可复制范式”表达偏弱。
- 相对 v4 的五条扩展亮点在当前 20 页中仅为分散覆盖：全链条场景见 P5/P15；三载体双链主见 P6；人才转型见 P7/P14；可信流通见 P15/P20；公共数据双底座基本未作为独立亮点呈现。
- 用户最终确认只做 DB-v1 的四条正式亮点，不恢复 v4 的五条扩展亮点。
- 最终结构扩为 23 页：P19 垂直深耕、P20 双链主协同、P21 数据-模型-场景-迭代闭环、P22 以数促转、P23 结语；P01-P18 内容保持。
- 输出：`I:\opc\平顶山市数据标注先行先试申报答辩_DB四亮点独立页_含解说备注.pptx`。
- 四页亮点各自使用真实产业/人才/工业平台/城市转型图片，样式保持官方红白与高密度结构。
- 23 页备注全部覆盖，每页 261-285 字符，平均 274.6；仅 notes 启用，音频/自动翻页关闭，fade 转场保持。
- 23 页 SVG gate 0 blocking；PPTX postflight 通过；基础版与备注版 PowerPoint 渲染 PNG 23/23 哈希完全相同。

---

# StoryDream VOX P0-3 发现（2026-09-05）

- `storydream-ui` 要求复用 `src/ui`、Lucide 与语义 tokens，并保持工作台选择状态由稳定 ID 驱动。
- 当前任务不触碰 Electron 后端或 sidecar；后端能力未就绪时只定义最小 UI 回调接口。
- 领域权威要求：`timeline.clips[]` 必须随策略和视频资产变化，预览不能继续使用固定 8 秒动画。
- UI 策略集合严格为 `deterministic-layers` 与 `living-poster`；遗留 `hybrid` 数据需要显式不可用状态，不能静默降级。
- 工作树中的 `DirectorDeskWorkspace.tsx`、`EditorialCollagePage.tsx`、`director-desk.css` 及多个测试已有并行修改，实施必须基于当前内容做小范围补丁。
- `editorial-collage.ts` 已声明三种 `renderStrategy`，但当前检索未发现 `videoJobId`；时间轴和视频资产的权威映射尚待审计。
- `DirectorDeskWorkspace` 已拥有 `playbackMs`、总时长、播放/seek 控制，改造应复用这套时钟，而不是引入第二个预览计时器。
- `EditorialCollageShot` 当前只有通用 `providerJobId`，没有 `videoJobId`；schema 同样缺失该字段。
- starter plan 为每镜建立 deterministic clip，`source: 'deterministic'`，资产来自 layer 的 `assetVersionId`；需要抽成可重复调用的权威同步函数。
- 目标工作台文件的现有脏改动主要是媒体加载态、批量队列和动作反馈，P0-3 补丁必须保留这些内容。
- `validateEditorialCollagePipeline` 当前只验证 beat/shot/layer/cue，一致性校验未覆盖 timeline clip 与策略/视频资产的对应关系。
- 现有测试只断言 starter timeline 为 30 秒，没有覆盖策略切换、`hybrid` 阻断、视频 Job/asset 绑定和 clip 同步。
- 现有工作台测试明确禁止原生控件，新增策略选择应使用 `SegmentedControl` 或 `SelectField`，状态按钮使用 `Button/IconButton`。
- 共享 `ProductionTimelineClip.source` 已支持 `deterministic | ai-video | local | mixed`，无需扩展基础生产合同。
- 标准工作流投影已采用“选中视频优先，否则图片；AI 视频 clip.source = ai-video”的先例，VOX 应遵循同一语义。
- `EditorialCollagePage` 当前 `generateShot` 只生成图片，`directorShotFromEditorial` 只查找首个 layer 图片；页面没有视频生成回调或视频预览映射。
- `DirectorDeskWorkspace` 已接收页面生成/渲染回调，适合增加可选的策略与 AI 动态回调，避免假装后端已经可调用。
- 页面 canonical selected ID 是 `selectedShotId`；`updateShot` 目前只映射通用导演字段，尚不能写入 VOX render strategy。
- 工作台 `playbackMs` 由 `requestAnimationFrame` 更新并在 clip 边界自动选择镜头；音频 seek 已使用镜头全局 offset，视频应接入同一偏移算法。
- 工作台当前总时长仍由 `shots.reduce(durationMs)` 推导，没有接收领域 timeline；若 shots 与 clip 保持强校验一致可复用，但预览素材/来源必须由页面映射后的权威 clip 状态提供。

## 2026-09-07 R06/U01 续核

- Provider job history is append-only; latest status must be selected by logical target, capability, episode, attempt, and timestamps. The new shared selector and page adapters now make a newer running retry authoritative over older completed/failed history.
- Director queue no longer invents waiting jobs from shot rows. Empty projects show no assets, no queue items, and explicit empty messaging; generated thumbnails are only shown when backed by a local asset path.
- Production Electron regression `I:/opc/.artifacts/director-desk-qa/report.json` passed with `runtimeErrors=[]` and `paidGenerationCalls=0`; the harness exits with return code 1 during cleanup, which is a known harness limitation.
# 2026-09-08 F05 续推

- 本次 QA 首轮失败原因：脚本使用生产 dist，但运行前没有重建；DOM 仍是旧版运动检查器。下一次先 build，再运行 Electron QA。
- 图层编辑需要覆盖尚未绑定图片的图层；全部隐藏时不能用 thumbnail 回退造成假预览。
- 运动编辑入口要区分本地关键帧和 AI 动态海报；AI 视频模式不应用本地层/相机参数。
- F05 操作已补生产保存重开、精确参数断言和正常/紧凑窗口截图；后续结构保存阻断修复后，2026-09-08 13:27 最终全流程 QA 通过。
- 结构编辑的 `reflowEditorialStructure` 错误更新本地 `updatedAt`，与存储层乐观锁冲突。新增真实数据库回归已先失败复现；修复为保存原有修订号，由存储成功提交时递增。保持旧修订拒绝写入与保存期间新增编辑保护。
- 回归另发现首段新增镜头可能超过现有 3 秒 hook 硬限制，导致保存校验失败。版本号专项先在允许扩展的第二段测试增删；首段结构可编辑与质量约束的冲突需单独处理。
- 页面完整诊断定位第二个结构错误：拆分首半镜头携带旧 `providerJobId`，但新 shot ID 与原 job.nodeId 不匹配。真实数据库测试已复现，修复为新镜头解除生成任务引用，原 jobs/assets 与图层图片引用全部保留。
- 更正此前“数据库已经保存 5 镜”的推断：失败时回读实际仍是 4 镜。Playwright `wait_for_function` 同步判断 async predicate 返回的 Promise，未真正等待异步条件；QA 已改用 `page.evaluate` 等待 Promise 结果再轮询，防止持久化假阳性。
- 最终生产 QA 已去掉强制隐藏遮罩的 DOM 操作，正常 Escape、保存、弹窗关闭、重载与重开通过。双尺寸截图中运动检查器独立滚动、关键控件可达，未见内容重叠；runtimeErrors=[]、paidGenerationCalls=0。

## 2026-09-08 F03/U02 结构与布局续推

- 新增时序回归首先复现四项失败：首段扩展导致后段词级时间越界、拆分保留越界 tokens、合并丢失第二镜头字幕偏移、排序未同步对齐信息。
- 拆分原实现缩放整段运镜，现改为按原时间区间采样并补边界关键帧；合并使用同时间点关键帧表达硬切换，预览与导出在边界使用后段状态。
- 后段/镜头移动采用既有漫剧对齐策略：等长平移同步 tokens 与指纹；剪短字幕则清除旧对齐和旧音频引用。三秒只保留起步模板设置；合法首段扩展可以保存。
- 镜头编号改为项目当前顺序；非法结构在写入页面前校验并显示反馈。长状态文本限宽省略，错误详情通过正常/紧凑窗口均可达的图标打开。
- 首轮类型检查发现联合数组泛型推断偏向图层帧，已显式标注相机/图层联合类型。专项初步 32 项通过，新增合并硬切前后实际导出 seek 对照和生产 UI 断言待运行。
- 后续还需处理多轨音频的结构编辑归属/裁剪与节拍增删及长文完整性验收，F03 总体保持进行中。
- 页面 QA 确认 `normalizeAppError` 默认不展示无错误码的原始 Error.message；合并图层/关键帧超限已补正式错误码，保持现有错误展示约定。结构捕获单独处理，避免吞掉运动检查器的本地错误。
- 2026-09-08 13:49 最终生产 QA passed，首段 3600ms 保存重开、01-05 连续编号、超限拒绝及错误弹窗均通过。正常与紧凑截图经过稳定动画处理后无重叠/截断；55 项聚焦回归、typecheck、build 和含同时间点跳切的真实 MP4 烟测通过。

## 2026-09-08 F03 音频结构编辑

- 新增 3 项回归复现：移动镜头后音频仍留在原项目时刻；拆分/合并后音频引用已删除的镜头。既有旁白还会因派生镜头清空 voiceAssetVersionId 而丢失。
- 实现镜头起点差值平移、拆分源文件区间、合并保留各段绝对起点，并在结构变化前显式保留旧版旁白。淡入淡出通过原始包络及片段偏移延续，预览和 FFmpeg 使用相同数据。
- 音源已结束但片段仍占时间线的尾部保留为 sourceDurationMs=0 的静音片段，避免重新读取源文件或复活旧旁白。新实现待单测、PCM 连续性与生产 UI 验证。
- 首批 53 项聚焦回归（含真实 FFmpeg 波形对照）通过。生产 QA 的 VOX 声音导出已通过，但漫剧发送 episodeId 被 director:render 严格 IPC schema 拒绝，已定位为接口契约遗漏，补可选分集 ID 和拒绝非法字段回归。
- 新 PCM 用例的 durationMs 从可选模型复制到必填 sidecar 参数时触发类型检查失败；夹具显式声明必填时长，切片结果在传入 sidecar 前展开该值。
- 生产声音 QA 于 14:11 passed：VOX/漫剧原混音导出、VOX 拆分/合并/新增移动/删除恢复、保存重开与预览源 seek 均通过，无运行错误或付费调用。1536x1024 与 1040x720 截图检查器控件可达，无横向溢出。
- PCM 逐采样比较已通过（拆分/合并差值不超过 2 个 16-bit 量化单位），但成片切点 RMS 比原片低约 4%，定位到逐镜头 AAC 尾部补样进入硬切拼接。追加实际 MP4 解码回归，需让硬切按镜头时长裁剪解码音频。
- PCM 夹具还存在 as const 的深层只读数组与 sidecar 可变数组不兼容；改为显式场景参数类型后 typecheck 通过。
- 收紧 MP4 解码测试前，夹具默认使用 fade 转场而实际导演导出使用 cut；显式对齐 cut 后复现切点 RMS 0.017434 对原片 0.018262 的偏差。
- 硬切改为分别连接画面和按镜头时长裁剪的音频，阻止 AAC 补样及非整帧画面时长影响声音时钟；解码音量回归通过。扩大 sidecar 测试发现单帧短场景缺少持续帧，补逐场景末帧延展后继续验证。

## 2026-09-08 F03 节拍与长文

- 原跨节拍移动只改 cue 的 `shotId`，没有保存源镜头内偏移；修复为基于源/目标 shot 起点重映射，并保留等长对齐指纹。
- 跨节拍回归必须使用有效音频资产版本和真实 aligned cue/token；仅用新增空镜头会让 ownership 检查失去意义。
- 起步长文原先每条 cue 只二分一次，长句仍可能过长；现在按最多 18 个字符并优先在标点切分，`sourceText` 与 cue 串分别持久化校验，覆盖 240 段中文输入。
- 节拍操作在桌面端分组显示，紧凑视口仍通过左栏抽屉访问；删除节拍沿用镜头删除的确认习惯。场景标签改用实际节拍标题，重排不会继续误称固定结构角色。
- 最新证据：聚焦 24/24、类型检查、生产构建、导演台 UI 21/21；`.artifacts/director-desk-qa/report.json` 为 `passed`，新增节拍实机操作及保存重开通过，`runtimeErrors=[]`、`paidGenerationCalls=0`。

## 2026-09-08 F02/F03 补验发现

- 原跨节拍实现只重映射移动镜头，reflow 却以已更改的源/目标顺序解释已有字幕；目标插入头部会截断既有 cue，反向删除头部会推迟剩余 cue。修复前回归真实复现，现使用源顺序先分别重排两个节拍，再统一全局起点。
- 原 FFmpeg 指标代码未检查返回码，缺值和空黑帧结果都被当通过；现分析状态独立，异常为 warning failure，缺少分析为 pending。严格验证数值完整性。
- 原审片摘要将 warning failure 也显示成阻断；长报告在 tabs 之前挤占固定区域。现准确统计、数值可见、长报告在检查器内滚动。
- 质量门实际失败被 catch 二次包装成“登记失败”，还可能追加重复报告；现保留已持久化的质量失败错误，不走登记失败包装。
- 全量媒体分析不能放在每次普通 probe 上，尤其导入长素材时；已增加 analyze_quality 可选标志，只在最终成片请求启用。
- 根计划中早先“长文完整性验收”只证明文本保留，没有证明阅读速度或最大输入可创建；F03 仍有待完成项。
- 已补生产证据：质量门拒绝当前版本时，同范围旧指纹资产仍可能带 selected/pinned。修复失败合并分支，只撤下已过期选中，保留有效同输入旧片和别集成片；回归与生产复跑通过。
- 新节拍原默认旁白“补充一段新的叙事内容。”会自动成为 cue；已移除自动正文，新增节拍为空稿，避免真实导出读出占位句。
- 长文边界已经实测：8000/12000 字虽远低于输入 schema 的 1,000,000 字上限，起步计划仍因每镜最多 100 cue 失败；4000 字可创建却被压成 60 秒的 224 cue，阅读速度不合理。原稿保留与成片可读性必须分别验收，不能扩大 cue 上限后直接宣布完成。
- 全文方案初稿还存在短文 60 秒拉长单 cue 超过 15 秒、英文分段丢失边界空格、组合字符拆分风险；已改为词边界分节、字素分 cue、cue 时长封顶及必要空白尾镜头。空白尾镜头渲染不得回填整节旁白。
- AI 文案原 30 秒建议 180-260 字，与阅读估计冲突；固定时长改为约每秒 3 字，全文改写明确保留完整事实与上下文。
- 生产长文 QA 初次通过后截图复审：224 镜头的最后一镜选中时，胶片条仍显示 01 起的列表；未生成镜头轮播无关示例图。已增加选中行/胶片 nearest 定位，胶片只使用真实缩略图或空态图标，并把分钟级时长改为可扫描格式。
- 已识别高容量的后续边界：文档资产/providerJobs 仍各最多 500 条，长项目多轮生成历史可能先耗尽该额度；本轮只验收长文规划/编辑/保存，不将 120 分钟 schema 容量等同于完整生成导出的已验证容量。
- 长批次的第二个 500 边界在 director:batch-create/update：300 镜头的图片+旁白+合成已有 601 节点，500 动态镜头有 1501 节点/1500 合成依赖。必须按领域实体区分容量，不能调整全局 IPC 数组上限。
- runDirectorBatchPlan 将全部状态快照排入 Promise 队列，未等待开始状态持久化就调用服务；工作台 onUpdate 另行排队且忽略所有持久化异常，长批次会积压大量完整快照并在保存失败后继续生成。
- Electron HTML 渲染目前先保存全部镜头所有 JPEG，再统一编码并清理，长成片的临时磁盘占用随总帧数增长；后续需逐镜编码/清理，保留固定时间轴与音轨语义。
## 2026-09-08 长项目批次执行复核

- 镜头容量与生成节点容量已拆开：500 个镜头最多对应 1501 节点，成片节点最多依赖 1500 个媒体节点；不扩大其他 IPC 数组。
- 执行器现在等待 running 快照落盘后调用服务、等待 completed 快照落盘后调度依赖；写入失败停止新派发并等待现有服务请求收尾。1501 节点模拟执行验证同时未落盘的状态转换不超过并发数 4。
- 父页面传入新的 batchPersistence 对象时，恢复 effect 原先会覆盖当前运行/故障状态；现增加活跃执行器和已加载记录的保护。
- 启动持久化期间明确显示“正在保存批次”，避免记录未创建时暂停/取消使用空批次 ID；取消后的最终状态按控制器取消意图落盘。
- 无执行器的故障批次取消前重新读取数据库，避免把未落盘的本地 running 标记当成实际已派发，保留数据库已完成项。
- 新增实际 SQLite 601/1501 节点重启恢复覆盖，已完成 300 项与全部依赖保留；当前聚焦 5 文件 41/41，通过。
- 生产故障 QA 仍在进行：首次注入在 Electron 初始化前获取 ipcMain 导致启动失败，已延迟到 setImmediate；随后脚本误找不存在的“队列”页签，已按真实生成检查器的队列区定位。
- 尚未解决资产/服务任务历史各 500 条上限，也未宣称长项目完整素材生成或长片导出已通过。

- 生产最终验证 21:10:07 passed：专门 Electron 测试入口包装实际 IPC 后加载 package.json.main，避免启动前 ipcMain 未初始化以及 setImmediate 时 IPC 已注册的问题。已确认注入命中，生成 IPC/网络均为零。
- 读取未落盘节点的本地 running 状态会造成“没有服务调用却持续转圈”的假象；故障显示改用最后落盘快照，已在两个视口复审。数据库仍为 running 的远端请求则显示“远端状态待核对”，不假装已终止。
- 同进程重新进入页面也可能没有可用执行器；恢复 UI 统一暂停并要求核对，不显示无人驱动的“批量生成中”。取消保留 completed 和未知 running，只取消 pending。
- 2026-09-08 历史容量生产验收发现：除了 assets/providerJobs 的 500 条上限，两个导演 schema 的 attempt 仍限 100；生成代码以同镜头同能力的历史记录数递增 attempt，所以放开数组后仍会在付费返回后保存失败。两工作流第 101 次图片生成单测先失败，随后将 attempt 限额和历史容量统一。
- 生成失败反馈原来仅挂在“生成”页签，配音页签的容量错误不可见。已接入公共状态区、可打开的错误详情和检查器提示；容量拒绝不应把以前完成的任务显示成失败。
- 4500 条生产历史下，生成记录/版本列表每页 20 项，素材展开每页 36 项；VOX 桌面/紧凑截图无横向溢出。全历史数据库仍保留，UI 仅分页展示；尚不代表 500 个镜头均调用真实远端服务。
- 长片资源改造定位：`renderHtmlVideo` 捕获全部镜头后才调用 `compose_render`；sidecar 本来逐镜头编码，因此可提取同一编码函数并在 Electron 镜头循环中调用。音频混音沿用 `scene_audio_input`，最终拼接保留当前硬切/转场语义。用户的全文时长选项已完成，本轮继续配套导出资源工作。
- 实际 HTML smoke 复现帧 seek 超时：`Promise.resolve(window.__tl.seek(...))` 会等待 GSAP 返回的 thenable 时间线完成；离屏渲染中时间线暂停，导致永久等待。真实 GSAP 专项先复现失败，再区分 `seek` 返回自身和真正异步媒体跳转；没有通过延长超时或跳过帧同步处理。
- 修复 seek 后 HTML smoke 继续暴露时长契约差异：运行时校验按转场后 1.5 秒，传给 sidecar 的目标却仍为 1.8 秒，低帧率输出变成 2 秒。调用渲染器前按已有转场编排计算目标；封面使用明确 coverDurationS，硬切不扣转场。最终 smoke 预期/实际均为 1.5 秒。
- 资源 QA 首轮：12 镜头各 15 秒、960x540/24fps，共 4320 JPEG，最大同时存在一个帧目录；图片峰值 3,628,259 字节、全帧累计 40,871,788 字节、临时目录峰值 9,879,832 字节。抽帧确认字幕 A/B 切换、运动标记、两处镜头切点和末镜正确；该低复杂度本地夹具不代表 500 镜头/46 分钟/1080p 的峰值。
- HTML runtime 的磁盘预检仍以全部帧预算，导演台没有相同预检；最终 FFmpeg 仍同时打开各片段。后续应基于更多实际素材测量编码片段、源片、BGM 和成片副本的空间，不能直接把磁盘预算改为只有单镜头 JPEG。
- 全文选项与默认值是不同验收项：此前 option 和恢复已完成，但页面 useState 仍为 30000。本轮改为 auto，并在生产 QA 明确验证输入长文后默认选中全文、无需切换即可继续；旧草稿恢复分支不变。
- 大批次实际生成 QA 使用 5 个节拍各 100 镜头，空素材/空任务开始，通过 UI 派发 4 并发、1000 图片/配音节点，响应仅来自现有本地媒体 fixture。最初约 44 秒已有 122 条完整素材与任务落盘，正在继续执行；该证据区别于此前直接填充历史数组的持久化验收。
- 22:46:37 大批次 QA 完成 1000 节点、1000 个唯一非空媒体文件（120,961,500 字节），初始图片/声音/时间线归属通过；重复末镜图片和配音保留全部历史。随后实际恢复旧画面触发 referencesValid 失败：页内 restoreVersion 只更新引用，不更新素材 selected；漫剧存在相同逻辑，需保留其他镜头/分集共享版本及 pinned 状态。
- 恢复专项首轮已通过 VOX 500 镜头新旧切换、1002 文件/任务保留及双尺寸；漫剧测试夹具漏改 scene.episodeId/shot ownership 被保存校验拒绝，修正夹具后继续。专项最初类型检查发现测试使用 local-image 而领域枚举是 local-file，已修正，typecheck/build 和 67 项回归通过。
- 最终恢复专项 22:57:14 passed；原 1000 节点完整批次和 1002 文件可复用，说明故障发生在恢复选中语义，而非文件落盘/历史容量。双工作流恢复后，引用资产已选中；共享图片按仍存在的镜头/分集/末帧/固定参考引用保留，历史与任务不删改。生产重启、跨项目和两个漫剧分集通过，6 张截图复审无版本区裁切。
- 长片空间预检原 HTML 按全片 JPEG 相加，导演台完全缺失；最终 sidecar 同时存在所有编码片段、源片和时长修正/成片副本。现按单镜帧/混音加全片编码副本估计，使用内容相关估计及余量，导演台暂存图层/视频按实际复制次数计入。157 项聚焦和 typecheck 通过，1080p/60 镜头/10 分钟真实渲染正在执行。
- 成片登记还存在整文件 readFile 后哈希的问题，长片会把整个 MP4 放入主进程内存。已改为复用 64KiB 缓冲分块计算并校验大小/文件身份未变，等待专项及生产登记复验。
- 磁盘生产专项最终 23:26:27 passed：两工作流成功输出的 SHA-256 与数据库/manifest 一致，注入 1024 字节可用后，复制次数和捕帧窗口数不增长，旧成片/素材完整且失败任务正确保存。修正 renderProject 的错误对象生命周期，详情只显示一个原诊断号。长片 60 输入最终拼接还在等待前置 14400 帧捕获完成。
- 实际 Windows 500 输入拼接诊断：当前 args 长 140,114 字符，启动立即失败 WinError 206；`.artifacts/compose-capacity-qa/report.json` 保留证据。将滤镜移入临时文件、以共同素材目录为 cwd 使用相对输入路径，保留拼接/采样算法，等待实际多输入和原切点回归。60 镜头长片测试使用启动时的独立 bundle，不受后续源码修改影响。
- 500 输入修复后的 32x32 实际成片已通过：命令 13,284 字符、125 秒、零滤镜残留；原 WinError 206 证据保存在 `before-fix.json`。大于 8 输入限制每个输入的解码线程为 1，但这不等于多输入资源恒定，也不代表 500 输入 1080p 已验证。初次回归缺少 Python tempfile 导入导致 4 项失败，补齐后 4 文件 32/32 通过。
- 补充实际转场测试发现 10 个 0.5 秒镜头原、新命令调用都会报 AAC 编码前 EOF；诊断 2/5 输入通过、10 输入失败。仅在 acrossfade 之后重分块无效，在每路输入重分块有效。现以 `asetnsamples=n=1024:p=0` 约束进入转场的音频帧，并按声明时长裁掉 AAC 填充、重置时间戳；3 项真实 Python/MP4 回归通过，10 镜头的音量、PCM 总长及硬切/转场新旧命令逐字节相同。
- 滤镜临时文件创建后写入仍可能因空间不足失败，必须在 finally 覆盖写入阶段；已注入实际文件写入异常验证清理，同时保留同目录其他任务的滤镜文件。相对输入路径、空格路径、独立输出目录和不修改原参数数组均已验证。
- 60 镜头/1080p/十分钟原 snapshot 在完成全部 14400 帧捕获及 60 段编码后，最终阶段触发外层 40 分钟超时，session 4151 exit 1；未到达最终抽帧/声音验收。最后采样全管线工作集峰值 6,412,484,608 字节、FFmpeg 6,226,874,368 字节、生成文件峰值 260,130,700 字节；这些是终止前采样而非成功完整输出的峰值。进程树已退出、临时根目录已清理；另外存档 60 个已完成片段用于最新代码复验。
- 原资源 QA wrapper 在超时时未更新报告，留下 running 假状态。已补 wrapper 失败落盘，原报告明确记录本次 PROCESS_TIMEOUT；最新拼接单独输出至 compose-long-1080p-qa，不覆盖失败证据。
- 最新源码 60 段合成于 23:52:13 passed：输出 108,906,375 字节、600 秒、1920x1080；拼接 155.64 秒，Windows API 记录 FFmpeg 峰值工作集 3,893,481,472 字节，命令长 2018 字符。8 个抽帧、60 个静音起点和 0/-6dB 交替音量通过；图像接触表确认照片运镜、A/B 字幕、镜头切点及末镜均正确。原超时测试为 CIM 采样峰值，当前为 Windows API 峰值，不能把二者写成严格性能对照。
- 新结果证明已捕获片段在最新拼接下能输出完整十分钟成片；没有重新运行整个 14400 帧捕获流程，也未验证 500 段 1080p。下一阶段需要限制同时进入最终图的片段数量，并验证时间戳、画质、PCM 和临时副本预算，不仅继续放宽命令长度或超时。
- 2026-09-09 硬切资源路径采用每 8 镜独立图：画面保持原 H.264 参数，只在分组边界重新开始 GOP；声音先写浮点 PCM WAV，最后单次 AAC，避免每组 AAC 填充累积。画面/声音分别用顺序 concat 输入，防止非整帧画面延长声音时钟；最终不再同时解码全部镜头。
- 分组暂存视频与源片占用原有两份输出估计；新增全片 44.1kHz/stereo/float32 PCM 及 WAV 头预算。17 镜头包含 0.11/0.19 等非整帧时长，实际帧数与原全图相同、PCM 样本总数相同且最大差 <=2；所有分组临时目录清理。
- 分组第一版在 239.9、319.9 等边界出现提前一帧；根因是分组 H.264 输出没有显式固定项目 FPS，末帧时间被 concat 舍入。加入 `-r` 后，24/30fps 非整帧专项通过；边界对照 14 个时间点的平均像素误差 0.62–1.04/255，PCM SHA-256 与单体输出一致。
- 最新 `.artifacts/compose-long-1080p-qa/report.json`（2026-09-09 00:25）使用最新分组代码：60 输入、9 个 FFmpeg 作业、每作业最多 8 输入、600 秒、8 个抽帧和 60 镜声音检查通过，FFmpeg 峰值 1,349,009,408 字节，耗时 154.39 秒。`.artifacts/compose-long-1080p-qa/boundary-report.json` 同批边界检查 passed。
- 500 个 32x32 输入在分组路径下命令 398 字符、64 个小作业、FFmpeg 峰值约 67.7MB、125 秒通过；这不代表 500 个 1080p 的完整编码质量或空间验收。低于产品固定 24fps 的 10fps 自定义专项曾出现不同帧舍入，不加入当前支持声明。
- 分组错误路径在缺失第二组素材时保持旧输出，临时组目录清空；第二组 FFmpeg 已产生 24 帧后取消，PID 退出、工作目录清理、旧输出保留。
- F02 音频证据增量：`media_quality_metrics` 现在在同一次 FFmpeg 分析中保留 volumedetect，同时读取 ebur128 的 integrated LUFS 和 true peak；数据已通过 `director-renderer`、主进程质量报告、VOX/漫剧 schema 和审片检查器显示。真实生产质量 QA 08:56:13 passed，证据示例为 -64.7 LUFS、-59.1 dBTP。
- 当前仍只把 LUFS/true-peak 当证据，不宣称门禁完成：低电平夹具会继续显示 warning 并允许导出；静音音轨 true peak 为负无穷时暂不写入有限数值；目标响度范围、真峰值阈值、静音解释和可执行局部重检仍待设计与回归。
- 2026-09-09 续核发现静音误判：缺少 LUFS 与低电平无法证明有意静音。改由成功的 ebur128 真峰值 -inf 显式记录 audioIsSilent，并省略静音 -70 LUFS 哨兵；缺少完整 ebur128 结果属于分析失败。显式静音混音与仍配置可听音频的全静音结果必须分开。
- 时间线检查须使用渲染请求的 episodeId；漫剧没有文档级 timeline。音频 durationMs 可省略，已有 validateProductionAudioTimeline 使用 sourceDurationMs/所属镜头回退，不能直接按缺字段判为异常。按产品范围接入响度 warning，不声称任何发布平台认证。
- 首轮审片截图复审：1536x1024 的阻断/警告计数与详情一致，1040x720 的音频阈值详情可换行，检查器滚动后底部重生成按钮可达；未发生横向溢出。该轮仅完成 VOX 画面，不代表漫剧/全静音新增状态已通过。
- 独立复跑新增状态截图：显式静音在紧凑检查器显示两行“全静音，不适用”，意外静音在桌面检查器显示声音配置不一致的 warning；底部审片操作完整可见。后续人工确认不能只改报告状态，需绑定成片输入指纹与分集，以防旧结论误用于新版本。
- F02 下一项代码核实：两个页面直接传 document.qualityReports，DirectorDeskWorkspace 按 createdAt 取最新；report schema 本身尚无 episodeId/renderFingerprint，虽然 provider job 和 output asset 已有这些字段。需要先补报告归属与过期状态，再做局部重检/人工确认，避免跨集或编辑后误展示旧通过结论。
- 最终构建的 VOX 真峰值超标截图显示 -0.4 LUFS、-0.2 dBTP，超出两条阈值的详情在 1040x720 检查器内完整换行。漫剧显式静音桌面截图显示全部结构检查通过、LUFS/真峰值不适用；检查器与素材区没有重叠。
- 最终生产专项 14:02:42 passed，VOX/漫剧静音与背景声/超峰值证据保持一致；48 张截图和 24 个检查器按钮命中/无横向溢出组合通过，漫剧紧凑超峰值详情复审无裁切。新报告存于 director-quality-policy-qa/attempt-3，之前的连接关闭和夹具阈值失败仍保留。
- F02 报告归属本轮实现：新成功/质量阻断/渲染失败报告均带 providerJobId、episodeId（漫剧）和 renderFingerprint；旧 director-quality ID 仅与确切 director-render-job ID 对应时恢复。按请求开始时间排序，优先显示当前输入的报告，元数据冲突或无法核对时不显示绿色通过；旧报告保留原始内容。
- 顶部审片步骤原来只依据 outputAsset，因此旧产物存在即可勾选；现需当前版本报告 passed 且检查项都已通过/豁免。导出步骤仍依据实际当前成片，警告或待人工检查不再伪装为已完成审片。
## 2026-09-09 实际字幕几何

- 实际 MP4 检测暴露高 DPI 下隐藏 CSS 视口为 1922x1082，而最终画布为 1920x1080；现有 capture 会整体缩放 NativeImage。字幕边界必须按 CSS 视口到输出画布比例换算，不能只放宽尺寸校验；证据同时保存原视口与成片尺寸。该失败保留在 subtitle-layout attempt-2。
- 标题与所有字幕 cue 共用两行网格，隐藏 cue 用 visibility 保留高度，避免正文长短改变时标题位置跳动。白底文字与半透明底板沿用现有样式，源换行不丢失，超长内容保留并报告越界。

## 2026-09-09 复核发现的待修缺口

- 最终生产结果：确认/导航 attempt-8 与归属 attempt-8 均 passed；代表性截图确认第二镜头第 2 句显示 1.400–3.000 秒相对时间，预览停在全片第 4 秒并显示对应字幕。抽屉固定在工作区，顶部返回不再被遮挡。人工确认成功仍保留机器待处理项，不将人工确认冒充机器通过。
- 确认保存采用非乐观分支，数据库成功结果相对保存前 baseline 合并；失败不写界面确认，等待期间新编辑保留。两次实机失败和延迟注入已记录到生命周期日志，错误展示使用用户可见脱敏消息。
- 新 QA 将待完成阻断检查与字幕/素材范围标注为 syntheticReviewScopes，原始实际成片报告先与 manifest 核对后才修改夹具。这验证操作与归属，不证明实际字体分析或局部媒体探针已完成。
- confirmation attempt-7 实际发现 1040x720 左侧镜头抽屉盖住顶部返回，Playwright 点击被“项目”标题截获；只有 <=900px 的 grid 声明 position:relative，901–1180px 抽屉定位到了整窗口。现统一给工作区 grid 建定位容器，并加顶部返回按钮命中断言。
- 复检解析器未从 cue/asset 反查镜头，且显式镜头与范围时间冲突时可能选中一个镜头却跳到另一个；字幕聚焦 effect 会在任何 cue 编辑后再次覆盖手动选句。
- 人工确认使用首条检查范围，未阻止 pending blocking 检查；页面先乐观写入确认再保存，失败时可能显示未落盘的确认。
- 先前 confirmation QA 的零付费计数为常量、溢出只采集未断言，未验证当前确认重启。旧报告 QA 删除元数据的步骤曾被移除，需要恢复实际推导覆盖。
- 新增诊断 attempt-6 的失败来自 QA 预加载时 Electron app 尚未初始化，已改独立主入口加载生产 main。

## 2026-09-09 局部重检可执行性

- 字幕实际测量已接入隐藏输出窗口。Windows DPI 会使 CSS 视口比编码画布略大，实测 1922x1082 对应 1920x1080；必须将文本/元素/标题/安全区坐标一并映射，保留视口原值供诊断，不能简单放宽尺寸比较。
- 排版可测性与字形覆盖是两类证据：当前可验证换行、长词折行、5% 安全区、标题和同时出现字幕的重叠，但字体加载成功并不证明所有中文字形存在；字形检查继续待人工复核。
- attempt-3 抽帧复审：横屏中文第一行/空行/第三行完整保留，竖屏英文长词正常折成两行；极长字幕实际越出画面，对应报告标为排版失败并可定位原句。紧凑窗口审片列表滚动到底后重新生成按钮可达，无横向溢出。
- attempt-4-overlap 实际碰撞画面与报告一致，四种模式/比例组合都同时标出两个 cue；相邻但不重叠的正常 cue 在 attempt-3 中无误报。该专项未修改生产 DOM/CSS，直接使用时间重叠的文档输入。
- 新前端缺口有截图和源码两类证据：`src/styles/features/director-desk.css:497` 固定编辑画布为 1.89，`DirectorDeskWorkspace.tsx:1334` 显示 projectTitle 和独立排版。实际竖屏 MP4 与编辑区的裁切、标题和字幕位置明显不同；下一批须统一比例和同一帧输出，不应把编辑区截图当作最终字幕排版验收。

- 质量报告已有 `shotIds/cueIds/startMs/endMs`，但只渲染为说明文字，用户无法快速回到受影响内容；现以纯函数解析范围并由工作区执行导航。
- 解析器只返回当前仍存在的镜头和 cue；项目级、无效对象和缺少局部执行 IPC 的情况都显示明确提示，避免把“定位”误报为局部渲染或媒体质量门已通过。
- 字幕检查器原本只在内部保存当前句，新增外部 `focusCueId` 后才能从审片报告跳到指定句；后续如新增人工确认，仍须绑定当前 reportId 与 renderFingerprint。

## 2026-09-09 人工复核记录

- 人工确认不能直接把报告状态改成通过，否则会掩盖新版本或跨分集误用；现只在 `directorQualityReview` 判定为当前且无阻断项时开放。
- 确认记录写入报告自身并经过两套工作流 schema；页面使用原有串行保存队列，避免与作者编辑或渲染结果覆盖。
- 初版确认 scope 取首条复检范围会漏记其他检查；本轮已改为整份当前报告的 project scope，分集由报告元数据限定。机器检查状态保持原样，不代表局部媒体探针已执行。
- 仅比较指纹不足以证明报告归属有效；确认前还必须通过当前分集的 `directorQualityReview`，否则缺失 provider job 或元数据冲突的历史报告仍可能被误确认。

## 2026-09-09 生产确认验收

- 独立 Electron 验收证明两个工作流的当前报告确认可持久化，编辑输入后确认状态变为 false，重启后历史确认记录仍在且不被 stale 报告误显示为已确认。
- 初版 confirmation attempt-2 只采集确认后的双尺寸截图，未断言确认按钮命中或溢出，零付费计数也未计算。本轮独立扩展真实点击、保存故障、当前确认重启与导航验收，结果另记。
- 生产验收后发现定位功能曾只切换到字幕页、未传递 cue 聚焦值；已补齐导演台到字幕检查器的 `focusedCueId` 接线，避免 UI 提示已定位但句子仍停留在默认项。

## 2026-09-09 编辑预览一致性补充

- 只改 aspect-ratio 不足以与导出一致：旧预览标题颜色、字体、字号上限、紧凑覆盖规则、画面遮罩、漫画边框和视频 overscan 都存在差异，播放条实际仍在画面内。现按输出画布宽度换算这些尺寸，并把播放条放到画幅之外。
- 导出为所有字幕保留网格高度，旧预览只渲染一条活动 cue，导致换句时标题移动且隐藏同时字幕碰撞。预览现保留所有 cue 的位置，仅切换可见性，和实际输出保持同一行为。
- 竖屏画幅收窄后，播放条若随之收窄会压缩控件；画面独立居中，播放条保留中央区域宽度。实际 1040x720 截图与按钮边界断言通过。
- 32 组真实预览/导出几何比较覆盖 VOX/漫剧、横竖屏、中文空行、英文长词和时间重叠；标题位置、字幕边界/字体和活动句数通过。测量使用归一化边界 0.8% 容差以容纳 Windows DPI 和小字号栅格差异，不表示截图逐像素相同。
- 两份最终 QA 报告均 passed、runtimeErrors=[]、paidGenerationCalls=0；当前仍需补长标题/空字幕/纪录片版式专项，不把这轮对齐延伸为全部视觉质量与字形完整性已通过。
## 2026-09-09 局部字幕复检实现核对

- 局部复检已具备独立 IPC 输入契约：任务、报告、分集、渲染指纹、镜头/cue 范围和期望版本时间戳均由调用方明确传入。
- 主进程开始执行前核对当前报告归属和指纹，保存前再次核对任务版本与指纹；局部渲染产生的输出只作为测量载体，成功后清理临时目录，不登记新的整片成片。
- `DirectorRenderScene` 不再暴露项目绝对起点，避免破坏既有对象契约；局部测量使用 `renderDirectorVideo.sceneStarts` 映射全局字幕时钟，完整渲染仍按场景顺序累计。
- 报告合并按原排版证据顺序替换已测镜头，再用完整当前场景重新计算字幕排版和字形两项检查，避免局部通过掩盖未选镜头的失败。
- 当前验证已覆盖 TypeScript、渲染/质量/IPC 53 项聚焦测试和生产构建；真实 Electron 局部复检的保存冲突、重启持久化、临时输出清理仍需下一轮专项 QA。
- 真实 Electron 专项 `.artifacts/director-quality-confirmation-recheck-qa-3/report.json` 已通过：两个工作流均执行局部复检，随后完成确认失败/重试、编辑后 stale 和重启持久化；报告保持原 ID，`runtimeErrors=[]`、`paidGenerationCalls=0`，局部渲染窗口在生命周期日志中正常创建/关闭。

## 2026-09-09 切点采样与旧复检纠偏

- 本轮重新类型检查发现工作区主进程仍读取 `Task.updatedAt` 和 `DirectorRenderSubtitleCue.cueId`，实际版本在文档 `updatedAt`、字幕 ID 为 `id`。已修正，并保留同镜头所有字幕排版上下文；此前日志不能替代当前源码验收。
- 未验收首版逐切点启动 FFmpeg、正则多转义、仅 64 段 schema、缺证据跳过、暗场硬阻断均已改正。当前每切点四帧，使用一次解码、实际 PTS、严格帧号/指标校验，最多 499 切点，近黑与同镜头亮度突变只作 warning。
- 首轮容量回归触及 4 MiB stderr 上限，保持 499 切点硬上限并将该专项日志限额设为 8 MiB；真实 499 切点复跑通过，命令长度小于 2048，滤镜文件无残留。
- 首次 Electron QA 已完成真实正常成片，脚本误把 100ms 步进滑块的 DOM value 当精确时钟；改验 React value 属性（实际为 2916.667ms），失败报告保留在 `.artifacts/director-visual-cuts-20260909-212157`。
- 旧字幕持久化测试假设起步规划总能生成同镜头两句字幕，当前输入不满足；测试改为显式构造两句，不修改起步规划行为。
- 复检失败清理补验发现双重 UUID 目录在 Windows 触发 `ENAMETOOLONG`；改用项目下 `.rq-XXXXXX` 短随机目录，`finally` 覆盖渲染前后并带删除重试。失败证据保留在 `.artifacts/director-visual-cuts-20260909-212651`。
- 最终验收 `director-visual-cuts-20260909-212907` 通过：4 次整片输出与 2 次字幕复检均为本地实际媒体；两个复检失败用例不改报告且无 `.rq-*` 残留。当前/过期两轮重启、切点报告 ID、manifest、双尺寸可达性均通过，runtimeErrors=[]、paidGenerationCalls=0、blockedNetworkCalls=0。

## 2026-09-10 U01/U05 页面续核

- 导演台左侧项目摘要仍直接引用 `preview-city.png`，这会让没有真实图片资产的空项目看起来已经有封面。修复后封面选择顺序为当前真实资产缩略图，再回退到镜头真实缩略图；两者都缺失时显示“未生成项目封面”，不能显示通用图。
- 创建向导的步骤操作栏属于外层滚动容器中的长表单底部动作。改为 `position: sticky; bottom: 0` 并使用实心表面背景，确保 1040x720 滚动时下一步/创建按钮可达，不改变表单字段或状态所有权。
- 生产 QA 夹具仍有独立问题：导演台完整脚本在未完成视频 fixture 上提前触发渲染前置失败；历史脚本启动 Electron 后没有页面对象。两者都不能替代新代码的双尺寸证据，需先修入口/fixture 生命周期再重跑。
- 紧凑窗口中队列副标题、视频任务 ID 和生成状态说明仍使用 9px；在不改变面板宽度和截断策略的前提下提升到 10px，避免状态信息成为不可扫描的小字。

修正记录：`qa-director-desk.py` 的视频归属夹具已修复并完成重跑；`.artifacts/director-desk-qa/report.json` 为 `status: passed`，空项目断言确认没有封面图和预览图。此前关于该脚本被 fixture 阻断的描述仅保留为历史过程，不再代表当前状态。
- 紧凑窗口中队列副标题、视频任务 ID 和生成状态说明仍使用 9px；在不改变面板宽度和截断策略的前提下提升到 10px，避免状态信息成为不可扫描的小字。

## 2026-09-10 U05 创建页续核

- 普通新建任务与 HTML 动画视频使用外层滚动容器，动作栏位于长表单末尾；在 1040x720 这类紧凑桌面窗口中，用户滚动编辑时容易失去下一步或提交入口。两者现已使用 sticky footer，背景与边界遮挡底层字段，移动/窄窗口动作按钮改为等宽或全宽。
- 音乐 MV 原先把生成按钮放在表头，长歌词和参数滚动后按钮会离开视口；表头现在在卡片滚动上下文内保持可见，窄窗口纵向堆叠以避免标题与按钮挤压。
- VOX/AI 漫剧音频导入的容量限制不能通过匿名内联回调表达，测试要求稳定的 `onImportSound={importSound}` 连接；容量预留已下沉到命名函数，保持行为不变。
- HTML 创建页 Electron 局部 QA 的失败发生在进入设置外观步骤，未进入创建表单；当前持久化窗口的设置导航在 CDP 查询中不可定位，不能把这次失败归因于 sticky footer，也不能据此宣称创建页真实截图通过。源码契约和 41 项聚焦回归已通过。
- 后续将局部 QA 明确限定为创建页范围，避免主题准备步骤遮蔽页面证据；最终 `.artifacts/html-video-create-sticky-qa/` 已取得 1320x860 与 1080x720 截图。真实 DOM 检查确认 `.hv-create-footer` 的计算 `position` 为 `sticky`，滚动到底部后仍在视口，横向溢出与裁切控件均为 0。
- `qa-workspace-unsaved.py` 续核同时覆盖 NewTask 与 Music MV：`E:/StoryDream-QA/workspace-unsaved/report.json` 记录 1536x1024/1040x720 两尺寸 sticky 动作栏、滚动高度与可见性，两个页面的横向溢出均为 0；该轮还复验保存失败、放弃、关闭重启恢复，`runtimeErrors=[]`。

## 2026-09-10 F02 字幕可见性负向专项

- 真实输出窗口的某个字幕边界被 QA 隔离生命周期故意返回错误 active cue；质量门没有把它当成成片阻断，而是将 `subtitle-visibility` 标记为 `pending`，写入当前镜头范围并在审片显示“可导出 · 待复核”。
- `.artifacts/director-subtitle-visibility-negative-qa/report.json` 覆盖 VOX/AI 漫剧横竖画幅、实际 MP4、双尺寸审片截图、测量失败和完整重启；这证明异常可见性会暴露给用户并可定位，不证明正常产品路径存在该异常。

## 2026-09-10 极端混排标题复核

- 中英日韩字符和超长英文词在当前普通/纪录片标题网格中均能完整落入安全区；横屏和竖屏的截图中没有标题裁切、越界或字幕重叠。
- QA 断言曾遗漏 `mixed-long-title` 的正常徽章分支，导致一次测试假失败；产品报告实际为通过，修正后 8 次真实渲染和重启流程通过。
- 该证据边界仍是排版几何与可见性：它不覆盖人物身份、镜头主体、场景关系或逐帧运动连续性，不能替代后续人工语义复核。

## 2026-09-10 U02 与长片验收脚本

- 导演台检查器仍有大量 9px 状态字，用户需要据此判断服务是否连接、视频是否就绪、批次节点处于何种状态；已将这些语义字段提升为 10px，阶段导航和非关键装饰辅助字未扩大。
- `qa-compose-capacity.ts` 的长片分支原来会把已有 60 个片段当成输入，导致 `--long-1080p` 名称与实际报告不一致。现在长片分支固定检查 500 个编号片段并写入预期输入数，避免误报容量通过。
- 现有 60 段、600 秒 1080p 合成报告仍只能证明中等规模的分组编码；500 段、5000 秒输出受磁盘和耗时约束尚未完成，继续保留为明确 pending。

## 2026-09-10 F03 阅读速度与长文创建

- 固定 15/30/60 秒模式仍在规划阶段阻断预计最低时长过长的原稿；全文模式保留全部正文并按 cue 最低阅读时长分配镜头，避免用截断文本“解决”时长冲突。
- 创建摘要现在显示有效字素数对应的最低预计字/秒和最长句字/秒。它是可解释的规划指标，实际旁白生成后仍必须以音频时长和对齐时间为准。
- 真实长文 QA 的紧凑窗口截图确认新增提示没有造成横向溢出，滚动内下一步操作仍可达；全文正文、字幕、镜头和重开数据保持一致。

## 2026-09-10 受控语义连续性复核

- 原视觉连续性 QA 的几何占位图无法支持人物/场景人工判断；现改为同一主体和同一街区的两张无文字图片，并在真实成片切点前后抽帧。
- 人工复核确认两个工作流正常样本中人物外观、伞、服装、位置和楼体窗户布局保持；亮度变化是夹具故意变化。黑帧负向样本不进入语义通过判断。
- 证据边界写入独立 JSON：单切点、两帧、受控本地资产。机器 YDIF 和该人工观察都不能替代真实 AI 素材的大样本人物身份与场景关系复核。

## 2026-09-10 F03 真实旁白时长回填第一步

- 旧链路只保存 TTS 文件路径，规划阶段的 cue 时长不能证明实际口播速度；现在 `ProductionAssetVersion.durationMs` 保存浏览器元数据探测到的本地音频时长。
- 生成旁白时先读取 `loadedmetadata`，探测失败不会把未知时长音频绑定到当前镜头；聚合旁白和独立对白都写入 `sourceDurationMs/sourceMediaDurationMs`，实际比规划长或短时保留差异，不自动拉伸字幕。
- `narrationAlignment` 报告逐镜头记录规划时长、实测时长、有效字素数、规划/实际字符每秒和差值。缺失时长为 `pending`，超过 250ms 且相对偏差超过 15% 为 `failed` 的人工复核项，未成为自动阻断。
- 导演台审片证据区显示“旁白时长对齐”，便于把真实 TTS 回填与字幕/镜头检查放在同一份报告里。
- 当前测试使用确定性音频时长夹具验证纯函数和绑定行为；尚未完成真实 Electron TTS 生成项目报告，因此不能把本轮当作真实服务质量或完整口播对齐验收。
# 2026-09-10 视觉连续性续核

- 现有四帧仅有平均/极值亮度和近黑比例，等平均亮度的画面跳变可能漏检；应以 FFmpeg signalstats YDIF 保存相邻帧逐像素亮度差，并避免把正常硬切误判为镜头内运动异常。
- `mapLocalVisualEvidence` 仅映射时间，未映射 index；原片非零起点的局部复检可能因此失败。选取不相邻镜头会生成原片不存在的临时切点，按时间范围删除旧证据又会删除没有真正复检的边界。
- 本轮先收口运动边界和局部复检映射，再继续字幕切换专项；不扩大为完整逐帧或人物语义连续性结论。

## 2026-09-10 F03 旁白对齐真实 QA 纠偏

- 审片证据不能只读取经过 `normalizeProductionAudioClips` 的可播放片段，因为该步骤会过滤显式静音；静音旁白仍需要保留其资产时长用于规划与实测对照。
- 同一个音频资产可以被拆分到多个镜头，资产的完整 `durationMs` 不能直接按镜头重复求和。只有明确的 `sourceDurationMs` 才覆盖资产默认时长，代表该片段裁剪后的有效源时长；没有源裁剪时仍以资产元数据为实测值。
- `qa-director-sound.py` 之前只用导出时断言对齐状态，报告没有落盘该证据；现在导出结果包含 `narrationAlignment`，可直接审计真实报告而不是依赖脚本内部异常。
- 最终声音专项 `.artifacts/director-sound/report.json` 已通过双工作流和结构编辑路径，且无运行错误、无付费调用。该证据覆盖确定性本地音频和真实 Electron 生命周期，不等同于第三方 TTS 服务质量或真实 AI 视觉连续性。

## 2026-09-10 500 段长片资源验收

- 500 段 1920×1080、每段 10 秒的硬切分组路径真实完成 5000 秒输出；分组最多 8 个输入，最终只做一次视频拼接和 AAC 编码，峰值 FFmpeg 工作集约 1.27GB，64 个合成进程，未留下 filter 文件。
- 500 个输入来自现有本地生成片段的编号复用，夹具保持颜色/音频模式和每段 10 秒边界，适合证明命令长度、分组释放、时钟和资源压力；不能据此证明 500 个独立 AI 生成镜头的内容差异、人物身份或场景语义。
- 原 15 分钟 QA 外层限制会在真实 500 段任务结束前提前终止，已只把 `qa-compose-capacity.ts --long-1080p` 的脚本上限调到 30 分钟；产品侧 `storybound-sidecar` 的单次媒体子进程限制未放宽。
- 转场路径仍不共享硬切分组策略，低帧率、自定义编码参数和编码中深阶段取消也未被这次硬切报告覆盖，继续保持 pending。

## 2026-09-12 R2/P02 新建入口

- 新建入口的统一类型合同位于 `TaskCreationTypePicker`，五种类型只在 `taskCreationTarget` 映射路由：智能成片留在三步表单进入内容步骤，VOX 到 `editorial-collage`，AI 漫剧、HTML 动画、音乐 MV 到同名真实工作区。
- 三步页面沿用 `material / creative / output` 稳定 stage ID，只调整用户可见语义，避免破坏既有字段清单、草稿恢复和 editorial QA 合同。
- MV 的提交门禁由 `MusicMvPage` 现有逻辑承担：歌词与 `musicMvAudioPath` 同时非空才可点击“生成音乐 MV”；BGM 不替代主歌曲。浏览器 QA 已验证无主歌曲时按钮禁用。

## 2026-09-12 R2.1 项目管理

- `ProjectHomePage` 原先将 pendingDelete 合入 ConfirmDialog.busy，弹窗刚打开即无法确认/取消；现只用实际异步提交状态锁定弹窗，失败信息显示在弹窗内。
- 查询加载状态不能禁用搜索框；200ms 合并查询并尊重 IME，旧回包由原 history controller 的 generation/epoch 隔离。返回列表复用路由持有的 controller 和筛选视图，同时重新查询当前页。
- 在 AppRoutes 同步导入分页 controller 会增加首屏体积并触发 bundle budget；改为项目页首次打开才初始化，保持该页 lazy load 边界。
- Electron QA 首轮的问题分别来自 IPC `{ ok, value }` 封装、隐藏窗口后 Chromium 停止稳定帧，以及将 Fluent tab 当作 button 定位；脚本已分别修正，完整通过证据写入独立 report。
- 五种类型用真实保存数据重开，普通/MV 到详情、VOX/漫剧到导演台、HTML 到其制作工作区。重启后仍有 499 条项目，删除记录未复活；这不包含付费流水线质量结论。

## 2026-09-12 联网技能发现与安装

- 用户希望直接寻找并安装若干联网技能，未指定数量与来源；默认选择少量、可信、用途互补的候选。
- 本机已经具备 `agent-reach`（多平台搜索/内容读取）和 `web-access`（浏览器/CDP 联网操作），因此候选应补充而不是简单复制这两类能力。
- 官方 `skill-installer` 默认从 `openai/skills` 的 `.curated` 清单安装，目标目录为 `C:/Users/foxnotail/.codex/skills`，同名目录存在时会主动中止而非覆盖。
- `agent-reach doctor` 显示公开网页、RSS、B 站搜索和 V2EX 已可用；GitHub CLI、Exa、YouTube 与多个登录态平台后端尚未配置。
- `web-access` 的 CDP 前置检查未通过：Node 20.19.5（建议 22+），且浏览器未开启远程调试。本任务不需要登录态浏览器，因此不要求用户改动浏览器设置。
- Firecrawl CLI 已安装但未认证，且本地缺少其引用的 `firecrawl-search` 子技能；不触发登录流程，转用可用的官方 Git 仓库与 Jina Reader。
- `git ls-remote` 已确认 `https://github.com/openai/skills.git` 的 `main` 分支可访问，故 GitHub API 403 只影响清单接口，不阻断后续安装。

## 2026-09-12 预览优先导演台

- 选定概念图的关键不是替换媒体，而是收紧信息层级：顶部项目栏与四阶段 rail 独立，中部 preview 获得最大稳定空间，检查器只显示当前镜头上下文。
- 既有工作区的业务步骤为七项，使用纯映射折叠成四阶段，阶段菜单继续暴露全部旧步骤，避免改变状态机和深层入口。
- 真实 Electron 截图显示旧版中央滚动会因选中胶片条而把预览标题顶出视野；选中镜头后改为仅调整胶片条横向 scrollLeft。
- 旧 QA 重载后直接查找历史按钮会失败，因为历史是“任务”主导航下的二级项；脚本现在先切换任务主入口再进入历史。
- 竖屏预览在紧凑窗口的最小高度/宽度已提高，但保留画幅比例和可滚动检查器；空态仍不伪造媒体。
