# 全应用界面审查发现

## 已知基线

- HTML 动画视频工作台已完成一轮独立修补，本轮不重复重做。
- 应用已采用浅色/深色主题、桌面侧栏、珊瑚色品牌强调色和 Lucide 图标。
- 目标用户需要高密度、稳定、可扫描的创作工作流。

## 待审页面

- 新建任务、任务队列、历史任务、任务详情
- 选书助手、对标导入、人物素材库
- 画图实验室、配音实验室、音乐 MV、爆款拆解
- 提示词模板、草稿模板、系统设置
- 账户中心、激活管理

## 视觉发现

- 错误详情弹窗曾同时绘制两个感叹号语义层，图标无法视觉居中；现保留单一 Lucide `AlertCircle`，颜色与危险状态令牌一致。
- 1080x720 设置页过早堆叠，导致模型选择控件离开首屏；现保留 190px 分类栏与内容双列，仅在 760px 以下堆叠。
- 账户页保存反馈与操作按钮缺少分组，本地信息混入表单层级；现分别组成操作行和独立信息行。
- 画图、配音和人物素材页面的空状态沿用有内容时的大画布，造成首屏失衡；现仅空状态收紧，有内容时不改变工作区尺寸。
- HTML 动画预览的响应式判断依赖窗口宽度，忽略其嵌在多栏工作台后的真实可用宽度；现由命名容器按组件宽度切换单双列。
- HTML 桌面端重新分配字幕参数与预览比例，首屏可同时看到画面、场景列表及逐场景播放；紧凑窗口保持单列，避免控件拥挤。
- HTML 预览 iframe 存在固有最小宽度，窄双列下会越过画布容器；显式设置 `min-width: 0` 和 `max-width: 100%` 后不再覆盖场景卡片。
- 珊瑚色实心控件使用近白小字时仅有 `3:1` 对比度；统一深墨色前景后保持品牌色且满足可读性。

## 审查能力

- `qa:editorial -- --scope=all` 可覆盖 17 个路由、深浅主题、1440x900 与 1080x720 两种窗口。
- QA 会采集横向溢出、可见控件裁切、主题令牌、文本对比度、媒体画布和关键交互状态。
- 页面分为 workflow、labs、system 三组，另有新建任务、任务操作、HTML 视频、克隆音色和火山 TTS 专项状态。
- HTML 专项最终桌面与紧凑截图位于 `C:\Windows\TEMP\storydream-editorial-artifacts-W0ewnM`，两种尺寸均通过自动门禁并完成目视检查。
- 最终 `all` 范围生成 92/92 张截图，桌面 49 张、紧凑窗口 43 张、浅色 52 张、深色 40 张；渲染错误、未解析令牌、图标命名缺口、工具提示缺口、文本对比度失败、焦点对比度失败、交互重叠和媒体失败均为 0。
- 最终全量截图位于 `C:\Windows\TEMP\storydream-editorial-artifacts-NozcK9`。
